# 验证策略与改进方法分析报告

> 基于 `alpha-beta-CROWN` 仓库的只读深度分析，2026-05-04
> 对应课程题目：_"对比不同验证策略的区别，并利用不同的验证策略在 MNIST、CIFAR-10 等数据集上验证网络在不同扰动半径下的鲁棒性。"_

---

## 1. 当前项目中的验证问题定义

### 1.1 形式化问题表述

当前项目验证的是**局部鲁棒性（Local Robustness）**，即对于单个输入样本 $x$，验证在其 $L_\infty$ 邻域内模型的分类结果是否保持正确。

验证目标可写为：

$$\forall x' \in B_\infty(x, \epsilon): \quad f_y(x') - \max_{j \ne y} f_j(x') > 0$$

其中 $y$ 是真实标签，$f_j(x')$ 是模型对类别 $j$ 的输出 logit。

### 1.2 代码中的具体实现

**配置文件中的问题定义**（[mnist_baseline.yaml](alpha-beta-CROWN/complete_verifier/mnist_baseline.yaml)）：

```yaml
specification:
  norm: .inf         # L∞ 范数扰动
  epsilon: 0.02      # 扰动半径 ε = 0.02
```

**代码中的 VNNLIB 规范生成**（[specifications.py:38-52](alpha-beta-CROWN/complete_verifier/specifications.py#L38-L52)）：

```python
class SpecificationVerifiedAcc(Specification):
    def construct_vnnlib(self, dataset, x_range, example_idx_list):
        for i in range(len(example_idx_list)):
            label = dataset['labels'][example_idx_list[i]]
            # C 矩阵：对每个 j≠y，构造行向量 [0,...,1_y,...,-1_j,...,0]
            c = (torch.eye(num_outputs)[label].unsqueeze(1)
                - torch.eye(num_outputs).unsqueeze(0))
            c = c[I].view(1, self.num_outputs - 1, self.num_outputs)
            # 对每个 j≠y: c[label]=+1, c[j]=-1, 要求 > rhs(=0)
```

代码将问题编码为**9 个并发 AND 约束**（对 MNIST 的 10 个类别，排除真实标签后 9 个不等式必须同时满足），形式为：

$$\bigwedge_{j \ne y} \left( f_y(x') - f_j(x') > 0 \right), \quad \forall x' \in B_\infty(x, \epsilon)$$

**输入扰动编码**（通过 `auto_LiRPA.perturbations.PerturbationLpNorm`）：每个输入像素在 `[x_i - ε, x_i + ε]` 范围内扰动（L∞ 范数），同时受数据范围 `[0, 1]` 约束。

### 1.3 验证结果判定逻辑

在 [complete_verifier_func.py:34-41](alpha-beta-CROWN/complete_verifier/complete_verifier_func.py#L34-L41) 和 [bab.py:565-567](alpha-beta-CROWN/complete_verifier/bab.py#L565-L567) 中：

```
结果三态：
  "safe"     → 所有 9 个约束的全局下界 > 0，证明鲁棒性
  "unsafe"   → 存在反例使得某个约束 ≤ 0，不鲁棒
  "unknown"  → 超时/超出搜索限制，无法判断
```

由此可见，当前项目验证的是：

| 验证维度 | 具体内容 |
|----------|---------|
| **验证对象** | 单个输入样本 $x$ 的局部邻域 |
| **扰动类型** | $L_\infty$ 范数球，半径 $\epsilon$ |
| **验证属性** | 分类不变性（true label logit > 所有其他 logit） |
| **验证类型** | 局部鲁棒性（local robustness），非全局 |
| **输出格式** | safe / unsafe (by counterexample) / unknown (by timeout) |

---

## 2. 当前已有验证策略分类

### 2.1 经验攻击类

#### 2.1.1 PGD 攻击

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ **存在，完整实现** |
| **所在文件** | [attack/attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py)（核心实现）<br>[attack/attack_interface.py](alpha-beta-CROWN/complete_verifier/attack/attack_interface.py)（接口封装）<br>[attack/general_spec_attack.py](alpha-beta-CROWN/complete_verifier/attack/general_spec_attack.py)（通用规范版本） |
| **核心函数** | `default_pgd_loss()`（attack_pgd.py:30-58）：hinge loss 计算<br>`attack_with_general_specs()`（attack_interface.py:63-79）：PGD 主循环 |
| **输入** | 原始输入 `x`、数据范围 `[data_min, data_max]`、规范矩阵 `C_mat`、右端 `rhs` |
| **输出** | `attack_ret` (bool): 是否找到反例；`adv_input`: 对抗样本 |
| **攻击方式** | L∞ 范数 PGD，支持 `uniform` 和 `osi`（optimal start initialization）初始化，支持 GAMA loss（Guided Adversarial Margin Attack） |
| **当前使用状态** | **课业实验中设为 `attack.pgd_order: skip`** — 未参与 M2/M3/M4 实验 |

**"只能证明不鲁棒，不能证明鲁棒"的原理：**

PGD 攻击的核心流程（[attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py)）：

```python
# 简化的 PGD 攻击流程
x_adv = x  # 从原始输入开始
for step in range(num_steps):
    x_adv = x_adv + α * sign(∇_x loss(f(x_adv), C, rhs))  # 梯度上升
    x_adv = clamp(x_adv, x - ε, x + ε)                    # 投影回 ε-球
    x_adv = clamp(x_adv, 0, 1)                            # 保持数据范围
```

PGD 本质上是在 $\epsilon$-球内的**非凸高维空间中进行梯度搜索**：
- 如果找到了违反 $\bigwedge_{j \ne y} f_y(x') - f_j(x') > 0$ 的 $x'$，则证明模型**不鲁棒**
- 如果没找到，可能是因为：(1) 模型确实鲁棒，(2) PGD 陷入局部最优，(3) 步长/步数不够。**无法区分这三种情况**
- 因此 PGD 提供的下界是"经验下界"而非"可证明下界"

#### 2.1.2 BaB-Attack（增强攻击）

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ 存在，但未被课业实验使用 |
| **所在文件** | [attack/bab_attack.py](alpha-beta-CROWN/complete_verifier/attack/bab_attack.py) |
| **核心思想** | 将 BaB 的分支定界框架用于攻击侧：分割输入域 + MIP 精化，比 PGD 搜索更全面 |
| **与 PGD 的关系** | PGD 快但不保证找到最优反例；BaB-Attack 慢但能找到更小的 margin 反例 |
| **当前使用状态** | 仅在配置 `bab.attack.enabled: true` 时启用，课业实验中未启用 |

#### 2.1.3 FGSM / AutoAttack

| 方法 | 状态 |
|------|------|
| **FGSM** | ❌ **当前仓库未发现独立实现**。PGD 退化为 1 step 时等价于 FGSM，但无独立文件 |
| **AutoAttack** | ❌ **当前仓库未发现相关实现** |

### 2.2 Bound Propagation 类

#### 2.2.1 CROWN（线性松弛边界传播）

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ **存在，是当前验证系统的核心基础** |
| **所在文件** | [beta_CROWN_solver.py](alpha-beta-CROWN/complete_verifier/beta_CROWN_solver.py#L41-L80)：`LiRPANet` 类包装 PyTorch 模型为 `auto_LiRPA.BoundedModule`<br>[incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py)：不完整验证入口<br>`auto_LiRPA/`（子模块）：底层边界传播引擎 |
| **核心机制** | 对每个线性层（Conv/Linear），使用线性松弛将 ReLU 激活替换为上下界线性包络 |

**CROWN 对 ReLU 的线性松弛方式（在 `auto_LiRPA` 底层实现，由 `LiRPANet` 调用）：**

对于 ReLU 神经元 $z = \max(0, \hat{z})$，已知 $\hat{z} \in [l, u]$：

- **如果 $l \ge 0$（稳定激活）**：$z = \hat{z}$（精确，下界斜率=上界斜率=1）
- **如果 $u \le 0$（稳定抑制）**：$z = 0$（精确，下界斜率=上界斜率=0）
- **如果 $l < 0 < u$（不稳定神经元）**：用线性包络代替
  - **下界**：$z \ge \frac{u}{u-l} \cdot \hat{z}$（凸包下界，tightest linear lower bound） 或 $z \ge 0$（松弛下界）
  - **上界**：$z \le \frac{u}{u-l} \cdot \hat{z} - \frac{u \cdot l}{u-l}$（凸包上界）

**传播过程：**

```
输入层
  │
  ▼
  Linear/Conv 层 → 通过前向传播计算上下界
  │
  ▼
  ReLU 层 → 根据 [l,u] 区间选择线性松弛
  │                   ├─ 稳定：精确传播
  │                   ├─ 不稳定：线性包络
  │                   └─ 抑制：置零
  ▼
  ...
  │
  ▼
  输出层 → 获得 f_j(x') 的上下界
            │
            ▼
            判定: lb[f_y - f_j] > 0 ∀j≠y → safe
                  ub[f_y - f_j] ≤ 0 ∃j≠y → unsafe (需要反例确认)
                  otherwise → unknown
```

**优点：**
- GPU 友好，可批量并行
- 对小 epsilon 速度快（ε=0.01 时 0.3-0.5s/100 样本）
- 可证明安全性（下界 > 0 时保证鲁棒）

**不足：**
- 线性松弛引入过近似，大 epsilon 下下界过于松弛
- 仅靠 CROWN（不完整验证）无法处理 ε=0.02 以上的大部分样本

#### 2.2.2 Alpha-CROWN（可学习参数优化版 CROWN）

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ 存在 |
| **所在文件** | [alpha.py](alpha-beta-CROWN/complete_verifier/alpha.py)：alpha 参数管理<br>[beta_CROWN_solver.py](alpha-beta-CROWN/complete_verifier/beta_CROWN_solver.py)：alpha 优化集成 |
| **核心思想** | 在 CROWN 的线性松弛中，不稳定 ReLU 的下界可以在"0 斜率下界"和"凸包下界"之间线性插值（由可学习参数 α 控制）。通过梯度下降优化 α 以最大化验证下界 |
| **CROWN vs alpha-CROWN** | CROWN 使用固定的下界选择策略；alpha-CROWN 通过优化自适应选择最佳下界 |
| **当前使用状态** | ✅ 在完整验证（BaB）流程中默认启用 |

#### 2.2.3 IBP（Interval Bound Propagation）

| 维度 | 描述 |
|------|------|
| **是否存在** | ⚠️ **代码存在但作为辅助手段，无独立验证实验** |
| **所在文件** | [domain_clipper.py](alpha-beta-CROWN/complete_verifier/domain_clipper.py)：使用 CROWN-IBP 边界进行域裁剪<br>[input_split/bounding.py](alpha-beta-CROWN/complete_verifier/input_split/bounding.py)：输入分割中的 IBP 边界 |
| **核心思想** | 对每个 ReLU 使用最粗粒度的 box 松弛（$z \in [0, u]$），逐层传播区间 |
| **当前使用状态** | 在完整验证中作为快速预筛手段，清理不可能的域。**未作为独立验证策略参与对比实验** |

### 2.3 Branch and Bound 类

#### 2.3.1 β-CROWN + BaB（当前实验核心方法）

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ **存在，是 M2/M3/M4 实验的核心验证方法** |
| **所在文件** | [bab.py](alpha-beta-CROWN/complete_verifier/bab.py)：BaB 主循环（`general_bab()` 函数，382-571 行）<br>[complete_verifier_func.py](alpha-beta-CROWN/complete_verifier/complete_verifier_func.py)：完整验证入口 `bab()`<br>[beta.py](alpha-beta-CROWN/complete_verifier/beta.py)：β-CROWN 分割约束参数 |

**BaB 主循环流程**（[bab.py:382-571](alpha-beta-CROWN/complete_verifier/bab.py#L382-L571)）：

```
general_bab(net, x, c, rhs):
  1. 初始化: 运行 CROWN/alpha-CROWN 获得初始上下界
     └─ ret = net.build_with_refined_bounds(...)
  
  2. 早期判定: 
     ├─ 若 global_lb > 0 → return "safe"
     └─ 否则 → 继续
  
  3. 创建初始域: domains = BatchedDomainList(...)
  
  4. BaB 主循环 (while num_domains > 0):
     ├─ 每轮: act_split_round()
     │   ├─ domains.pick_out(batch)     ← 取出当前批次域
     │   ├─ split_domain()              ← **关键步骤：分支**
     │   │   ├─ 获取分支决策 (heuristic)
     │   │   │   └─ branching_heuristic.get_branching_decisions(d, split_depth, ...)
     │   │   ├─ net.build_history_and_set_bounds(d, split)
     │   │   │   └─ 将分割约束注入边界传播 (β-CROWN)
     │   │   ├─ net.update_bounds()     ← alpha-CROWN 优化
     │   │   └─ domains.add(ret)        ← 将新子域加入队列
     │   └─ 检查终止条件
     │       ├─ 所有域已判定 → return "safe"
     │       ├─ 超时 → return "unknown"
     │       ├─ 超过最大域数 → return "unknown"
     │       └─ 发现反例 → return "unsafe_bab"
     └─ 继续下一轮
  
  5. 返回: (global_lb, visited, result, stats)
```

**分支类型：**

| 分支类型 | 描述 | 所在代码 |
|---------|------|---------|
| **ReLU split** | 对不稳定 ReLU 神经元进行分支：分支为 $z=0$（抑制）和 $z=\hat{z}$（激活） | [bab.py:50-189](alpha-beta-CROWN/complete_verifier/bab.py#L50-L189) `split_domain()` |
| **Input split** | 对输入空间的像素值范围进行分支 | [input_split/](alpha-beta-CROWN/complete_verifier/input_split/) |

**分支策略（Branching Heuristics）：**

| 策略名 | 实现位置 | 核心思想 | 课业实验使用 |
|--------|---------|---------|------------|
| **babsr** (baseline) | [heuristics/babsr.py](alpha-beta-CROWN/complete_verifier/heuristics/babsr.py#L163-L255) | 基于 α-scoring（lA 加权） + 截距备份评分，选择对边界收紧贡献最大的 ReLU 分裂 | ✅ M2 `baseline`/`auto` |
| **fsb** | [heuristics/fsb.py](alpha-beta-CROWN/complete_verifier/heuristics/fsb.py#L22-L213) | Filtered Smart Branching：在 babsr 基础上逐层过滤候选，用 bound propagation 验证每个候选的实际效果 | ❌ 未使用 |
| **kfsb** | [heuristics/kfsb.py](alpha-beta-CROWN/complete_verifier/heuristics/kfsb.py#L22-L275) | k-Filtered Smart Branching：fsb 的批量版本，用 top-k 候选同时评估，选择最优分裂 | ✅ M2/M3/M4 `kfsb` |
| **random** | [heuristics/base.py](alpha-beta-CROWN/complete_verifier/heuristics/base.py#L208-L216) | 随机选择不稳定神经元分裂 | ❌ 未使用 |
| **intercept** | [heuristics/base.py](alpha-beta-CROWN/complete_verifier/heuristics/base.py#L219-L239) | 仅基于截距项评分 | ❌ 未使用 |

#### 2.3.2 β-CROWN 的核心贡献

β-CROWN（[beta.py](alpha-beta-CROWN/complete_verifier/beta.py) + [bab.py](alpha-beta-CROWN/complete_verifier/bab.py) 中的 `build_history_and_set_bounds`）的核心创新：在 BaB 过程中，将**分支决策（split constraints）作为额外约束编码到 alpha-CROWN 的优化问题中**，使得子域上的边界传播可以利用父节点的分支信息，进一步收紧边界。

```
传统 BaB (CROWN-only):
  split → 在子域上重新跑 CROWN → 边界仅能利用子域信息

β-CROWN (当前实现):
  split → 分割约束编码为 β 参数 → alpha-CROWN 优化同时考虑 α (松弛参数) 和 β (分割约束) → 更紧的边界
```

### 2.4 Solver 精确验证类

#### 2.4.1 LP/MIP 求解

| 维度 | 描述 |
|------|------|
| **是否存在** | ✅ 存在（需要 Gurobi 许可证） |
| **所在文件** | [lp_mip_solver/](alpha-beta-CROWN/complete_verifier/lp_mip_solver/) |
| **核心内容** | `mip_core.py`：MIP 编码（ReLU 用 Big-M 方法编码为 0-1 整数变量）<br>`bounds_core.py`：LP 边界精化<br>`refine_core.py`：中间层 MIP 精化 |
| **当前使用状态** | 在 BaB 中作为可选精化步骤（配置 `bab.cut.enabled: true` 和 `solver.beta-crown.all_node_split_LP: true`），**课业实验中未显式启用** |

#### 2.4.2 Gurobi / SCIP / CPLEX

| 求解器 | 状态 | 说明 |
|--------|------|------|
| **Gurobi** | ✅ 有接口 | [lp_mip_solver/](alpha-beta-CROWN/complete_verifier/lp_mip_solver/) 使用 Gurobi API，需学术许可证 |
| **CPLEX** | ✅ 有割平面模块 | [cuts/CPLEX_cuts/](alpha-beta-CROWN/complete_verifier/cuts/CPLEX_cuts/) 用于生成 MIP 割平面 |
| **SCIP** | ✅ 有接口 | [scip_model.py](alpha-beta-CROWN/complete_verifier/scip_model.py) 提供了 SCIP 求解器接入 |
| **Marabou** | ❌ **无直接接入** | 仅在文档和 VNN-COMP 配置中被提及（如 `marabou_cifar10.yaml`），无实际运行 |

#### 2.4.3 ONNX 导出 → Marabou 接入条件

| 条件 | 状态 |
|------|------|
| **ONNX 模型** | ✅ 已有：`saved_models/mnist_fcnn.onnx` |
| **VNNLIB 规范** | ✅ 代码可自动生成（[specifications.py](alpha-beta-CROWN/complete_verifier/specifications.py)） |
| **Marabou 安装** | ❌ 未安装 |
| **Marabou 接入脚本** | ❌ 不存在 |
| **结论** | **具备接入 Marabou 的基础条件**（ONNX + VNNLIB 格式均支持），只需安装 Marabou 并编写调用脚本 |

---

## 3. 不同策略之间的对比

| 方法 | 类型 | 能否证明鲁棒 | 速度 | 精确性 | 可扩展性 | 适合 MNIST | 适合 CIFAR-10 | 当前仓库支持程度 |
|------|------|------------|------|--------|---------|-----------|-------------|---------------|
| **PGD 攻击** | 经验评估 | ❌ 只能证否 | ★★★★★ (毫秒-秒) | — | ★★★★★ | ✅ | ✅ | ✅ 代码完整，但课业实验中 skip |
| **IBP** | 形式化-不完整 | ✅ 但过于保守 | ★★★★★ (毫秒) | 低 | ★★★★★ | ✅ (小ε) | ✅ (小ε) | ⚠️ 辅助代码存在，无独立评估 |
| **CROWN** | 形式化-不完整 | ✅ 但ε大时无法证明 | ★★★★ (秒) | 中 | ★★★★ | ✅ | ✅ | ✅ 核心组件 |
| **α-CROWN** | 形式化-不完整 | ✅ 比CROWN更紧 | ★★★ (秒-分) | 中高 | ★★★ | ✅ | ✅ | ✅ 核心组件 |
| **β-CROWN + BaB (babsr)** | 形式化-完整 | ✅ 理论上可证明所有 | ★★ (分) | 高(收敛到精确) | ★★ | ✅ (ε≤0.02) | ⚠️ 未测试 | ✅ **课业实验 baseline** |
| **β-CROWN + BaB (kfsb)** | 形式化-完整 | ✅ 比babsr更强 | ★★ (分) | 高 | ★★ | ✅ (ε≤0.03) | ⚠️ 未测试 | ✅ **课业实验最优策略** |
| **Gurobi MIP** | 形式化-精确 | ✅ 精确 | ★ (小时) | 精确 | ★ (小型网络) | ✅ (极小网络) | ❌ (规模受限) | ⚠️ 需许可证，未使用 |
| **Marabou (SMT)** | 形式化-精确 | ✅ 精确 | ★ (小时) | 精确 | ★ (中小网络) | ✅ | ✅ | ❌ **未接入** |

**核心 trade-off 总结：**

```
证明能力:  PGD << IBP << CROWN << α-CROWN << β-CROWN+BaB << MIP/Marabou
速度:      PGD >> IBP >> CROWN >> α-CROWN >> β-CROWN+BaB >> MIP/Marabou
可扩展性:  PGD ≈ IBP > CROWN > α-CROWN > β-CROWN+BaB > MIP/Marabou
```

**关键洞察**：当前项目实验的 BaB 方法（baseline/kfsb）位于"速度-精确性" Pareto 前沿的拐点位置，适合中等规模网络（如 MNIST FCNN 3层）在中等 epsilon（≤0.03）下的验证。

---

## 4. 扰动半径 epsilon 实验设计

### 4.1 当前仓库已支持的 epsilon 实验

**M4 实验**（[complete_verifier/exp_configs/course/m4/](alpha-beta-CROWN/complete_verifier/exp_configs/course/m4/)）已完成 3 策略 × 4 epsilon 的网格：

```
MNIST (L∞):
  ε ∈ {0.01, 0.02, 0.03, 0.05}  ✅ 已完成
  样本: 0-100 (前 100 张测试图)
  配置: 12 个 YAML 文件
  运行: run_m4_epsilon_grid.sh
```

**实验结果概览**（来自 [项目书/results/m4/m4_epsilon_grid.csv](alpha-beta-CROWN/项目书/results/m4/m4_epsilon_grid.csv)）：

| ε | baseline VRA | kfsb VRA | kfsb Mean Time | kfsb Timeout |
|---|-------------|----------|---------------|-------------|
| 0.01 | 100.0% | 100.0% | 0.31s | 0 |
| 0.02 | 91.0% | 92.0% | 3.27s | 8 |
| 0.03 | 65.0% | 68.0% | 6.66s | 32 |
| 0.05 | 12.0% | 11.0% | 11.38s | 89 |

### 4.2 推荐补充的 epsilon 实验

**当前不足**：ε=0.02 到 ε=0.03 之间有较大的性能跳变（91%→65%），缺少细粒度数据点。

**推荐 MNIST 补充实验：**

| ε | 理由 | 推荐策略 | 样本量 |
|---|------|---------|-------|
| 0.015 | 填补 0.01-0.02 间隙，观察从 100% 下降到 91% 的转折点 | 全部 3 种 | 100 |
| 0.025 | 填补 0.02-0.03 间隙，观察从 92% 下降到 68% 的转折点 | kfsb (最优) + baseline | 100 |
| 0.04 | 填补 0.03-0.05 间隙 | kfsb | 100 |

**推荐 CIFAR-10 实验（当前完全缺失）：**

| ε (L∞) | ε (对应像素值) | 理由 | 推荐策略 |
|---------|---------------|------|---------|
| 1/255 ≈ 0.004 | 1/255 | 极小扰动，预期高 VRA | 全部 3 种 |
| 2/255 ≈ 0.008 | 2/255 | 标准最小实验半径（CROWN-IBP 论文常用） | 全部 3 种 |
| 4/255 ≈ 0.016 | 4/255 | 中等扰动 | kfsb (最优) |
| 8/255 ≈ 0.031 | 8/255 | 较大扰动，查看可扩展性 | kfsb (最优) |

### 4.3 每个实验半径应记录的关键指标

根据课程实验已有的统计模式（[summarize_m4_results.py](alpha-beta-CROWN/项目书/scripts/summarize_m4_results.py)），建议对每个 (策略, ε) 组合记录：

| 指标 | 含义 | 来源 |
|------|------|------|
| **Clean Accuracy** | 模型干净准确率 | `cifar_eval.py` 或训练日志 |
| **Verified Robust Accuracy (VRA)** | `safe_count / total_count × 100%` | abcrown 日志 `Final verified acc` |
| **Attack Success Rate** | PGD/攻击找到反例的样本比例 | 攻击日志（当前为 skip） |
| **Timeout Count / Rate** | 超时样本数/率 | 日志 `timeout` |
| **Safe Count** | 已证明安全的样本数 | 日志 |
| **Unsafe Count** | 已证明不安全（含攻击找到反例）的样本数 | 日志 |
| **Unknown Count** | 超时未判定的样本数 | 日志 |
| **Mean Verification Time (s)** | 平均验证时间 | `summarize_m4_results.py` 提取 |
| **Max Verification Time (s)** | 最大验证时间 | 日志 |
| **Total Wall Time (s)** | 实验总耗时 | Shell 脚本计时 |
| **Node Visits (last/max)** | BaB 节点访问数 | M3 实验额外统计（[summarize_m3_nodes.py](alpha-beta-CROWN/项目书/scripts/summarize_m3_nodes.py)） |

---

## 5. 当前改进方法是否成立

### 5.1 判断：**成立，且有对照实验数据支撑**

当前项目确实实现了一种"更优的验证思想"：**kfsb 分支策略 + 扩展候选池（candidates=5）**。

### 5.2 严谨分析

#### 改进名称

**KFSB with Extended Candidate Pool (kfsb_candidates5)**

#### 改进目标

在 BaB 完整验证中，通过优化分支选择策略来提高验证成功率（VRA），同时不增加（甚至降低）平均验证时间。

#### 改进前流程

```
baseline (babsr) 分支选择:
  1. 对所有层的所有不稳定 ReLU 神经元计算 babsr_score
     ├─ alpha_score = |bias_candidate + intercept_candidate| (lA 加权)
     └─ intercept_score = (备选，仅当 alpha_score 无效时使用)
  2. 选 top-3 (candidates=3) 高分的神经元
  3. 对每个候选做一次 bound propagation 评估
  4. 用 reduceop=min 选出最优分裂点
  5. 在最优分裂点上做 ReLU split
```

#### 改进后流程

```
kfsb + candidates=5 分支选择:
  1. 对所有层的所有不稳定 ReLU 神经元计算 babsr_score
     ├─ alpha_score (同 baseline)
     └─ intercept_score (同 baseline)
  2. 选 top-K (K=5, 比 baseline 多 67%) 高分的神经元候选
  3. 对每个候选做一次 bound propagation 评估 → 得到改善的 lower bound
  4. 同时考虑 alpha_score 和 intercept_score 两个评分来源
  5. 用 reduceop=min 选出使下界改善最大的分裂点
  6. 在最优分裂点上做 ReLU split
```

**关键差异：**

| 维度 | baseline (babsr) | kfsb_candidates5 |
|------|-----------------|------------------|
| 候选数量 | 3 | **5** |
| 评分来源 | babsr α-score + 备选截距 | babsr α-score **并行**截距评分，取两者更优 |
| 层间比较策略 | 跨层 top-k 比较 | 跨层 top-k 比较 + 逐层过滤 |
| 节点访问 | 13014 (last) | **72224 (last)** |
| 效果 | 91.0% VRA, 4.06s, 9 timeout | **93.0% VRA, 3.24s, 7 timeout** |

#### 理论动机

1. **候选池扩大**：从 3 个候选扩展到 5 个，增加了找到"真正最优"分裂点的概率。babsr score 是对分裂效果的一阶近似，更多候选弥补了近似误差。

2. **双评分机制**：kfsb 同时评估 alpha_score（基于 lA 梯度信息）和 intercept_score（基于截距的几何信息），互补了两类信息源的盲区：
   - alpha_score 对激活状态敏感，但对边界极值不敏感
   - intercept_score 对几何信息敏感，但对梯度信息不敏感

3. **验证驱动选择**：babsr_base_score 只是"预测"哪个分裂更好；kfsb 的关键创新是**实际运行 bound propagation 来评估每个候选**，从而选择真正改善下界最大的分裂点。这属于"lookahead"策略。

#### 代码实现位置

| 组件 | 文件 |
|------|------|
| KFSB 分支策略（算法实现） | [heuristics/kfsb.py](alpha-beta-CROWN/complete_verifier/heuristics/kfsb.py#L22-L275) |
| BaBSR 基础评分 | [heuristics/babsr.py](alpha-beta-CROWN/complete_verifier/heuristics/babsr.py#L36-L161) `babsr_score()` |
| BaB 主循环（调用分支策略） | [bab.py](alpha-beta-CROWN/complete_verifier/bab.py#L91-L95) |
| 最优配置（`kfsb_candidates5`） | [exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml) |

#### 是否有对照实验

✅ **有完整的对照实验（M3 消融实验）**

在 [项目书/results/m3/m3_branching_ablation.csv](alpha-beta-CROWN/项目书/results/m3/m3_branching_ablation.csv) 中，5 种配置进行了 AB 比较：

| 配置 | 变量控制 | VRA | Timeout |
|------|---------|-----|---------|
| baseline | babsr, candidates=3, reduceop=min | 91.0% | 9 |
| auto | babsr, 自动参数 | 91.0% | 9 |
| kfsb | kfsb, candidates=3, reduceop=min | 92.0% | 8 |
| kfsb_reduceop_max | kfsb, candidates=3, **reduceop=max** | 92.0% | 8 |
| kfsb_candidates5 | kfsb, **candidates=5**, reduceop=min | **93.0%** | **7** |

消融变量：策略类型（babsr→kfsb）、reduceop（min→max）、candidates（3→5）。

#### 是否能写入课程报告

✅ **可以直接写入**。改进方法有：
- 清晰的理论动机（候选扩大 + 双评分 + lookahead）
- 完整的代码实现（可追溯、可复现）
- 严格的 AB 对照实验（单变量控制）
- 量化的改进效果（VRA +2.0%、timeout -22%、mean time -20%）
- 代价分析（节点访问增加 5.5×，但总时间反而减少）

---

## 6. 推荐的低成本改进方案

### 6.1 四个方案对比

| 方案 | 实施难度 | 预期改进 | 与当前代码兼容性 | 课业报告价值 |
|------|---------|---------|---------------|------------|
| **A: Attack-guided Verification** | 低 | 节省 10-30% 验证时间 | 高（PGD 代码完整） | 高 |
| **B: Two-stage Verification** | 中 | 节省 20-40% 验证时间 | 中（需改流程） | 高 |
| C: Adaptive Epsilon | 中高 | 提供样本级分析 | 低（需大量改造） | 中 |
| D: ReLU Stability Branching | 中 | 可能提升 VRA 1-3% | 中（需改 kfsb） | 中 |

### 6.2 推荐方案：**方案 A（Attack-guided Verification）+ 方案 B（Two-stage Verification）组合**

**选择理由：**

1. **方案 A（PGD 预筛选）**：PGD 攻击代码已在 [attack/attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py) 完整实现，当前实验仅需将 YAML 中的 `attack.pgd_order: skip` 改为 `attack.pgd_order: before`。**基本零代码改动**。

2. **方案 B（IBP/CROWN 快速预筛）**：IBP 和 CROWN 不完整验证代码均已在 [incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py) 中实现，当前 BaB 流程的第一步实际上已经在做这件事（`general_bab` 开头运行 CROWN 获得初始界）。只需**增加对比实验**即可。

3. **两个方案的组合效果**：形成了 **"attack pre-filter → fast bound propagation → precise BaB"** 的三层分流验证流水线：
   ```
   输入样本 x
     │
     ├─ [Layer 1: PGD Attack]  ← 方案 A
     │   ├─ 找到反例 → "unsafe-pgd" (≈0.1s)
     │   └─ 未找到 → 进入 Layer 2
     │
     ├─ [Layer 2: CROWN/IBP]    ← 方案 B
     │   ├─ 下界 > 0 → "safe-incomplete" (≈0.5s)
     │   └─ 无法判定 → 进入 Layer 3
     │
     └─ [Layer 3: β-CROWN + BaB + kfsb]  ← 现有方法
         ├─ 证明安全 → "safe-bab" (≈3s)
         ├─ 找到反例 → "unsafe-bab"
         └─ 超时 → "unknown" (30s timeout)
   ```

### 6.3 方案 A 详细实施计划

**修改文件：**

| 文件 | 改动 |
|------|------|
| [exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml) | 将 `attack.pgd_order: skip` 改为 `attack.pgd_order: before` |
| 新增/复制配置文件 | 为 M2/M4 实验分别创建 `*_with_attack.yaml` 变体 |
| [项目书/scripts/](alpha-beta-CROWN/项目书/scripts/) | 新增 `run_attack_guided_verification.sh` |

**预期改进指标：**

- **PGD 预期命中率**（ε=0.02 MNIST）：基于文献经验，PGD 通常能对 70-85% 的不鲁棒样本找到反例。已知 M4 中 ε=0.02 时 kfsb 有 92% VRA，即 8% 样本无法证明安全。这 8% 中大部分（估计 5-7%）可被 PGD 快速标记为 unsafe。
- **时间节省**：PGD 攻击 ≈ 0.05s/样本，BaB 验证 ≈ 3.27s/样本。若 PGD 能预筛 5% 样本，节省 ≈ 5×30s ≈ 2.5 分钟/100 样本（但要注意 PGD 本身开销）。

### 6.4 方案 B 详细实施计划

**修改文件：**

| 文件 | 改动 |
|------|------|
| [incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py) | 已有 `incomplete_verifier()` 函数，无需改动 |
| 新增对比配置 | 创建仅运行不完整验证的 YAML 配置（`complete_verifier: incomplete`） |
| [项目书/scripts/](alpha-beta-CROWN/项目书/scripts/) | 新增汇总脚本对比不完整验证 vs 完整验证 |

**预期改进指标：**

- 对 ε=0.01：CROWN-only 预期达到 95-100% VRA（与 BaB 相同或接近），但速度快 10-100×
- 对 ε=0.02：CROWN-only 预期达到 60-80% VRA（BaB 为 92%），但速度快 10×
- 关键收益：识别哪些样本是"低垂果实"（仅需 CROWN 就能证明安全），减少不必要的 BaB 开销

### 6.5 方案 A+B 组合的 Ablation 设计

```
实验设计（固定 MNIST, ε=0.02, 样本 0-100）:

  C1: BaB-only (kfsb_candidates5)                     ← 基准
  C2: PGD-pre-filter + BaB (方案 A)                    ← C1 vs C2: 衡量 PGD 预筛效果
  C3: CROWN-pre-filter + BaB (方案 B)                   ← C1 vs C3: 衡量 CROWN 预筛效果
  C4: PGD + CROWN + BaB (方案 A+B 组合)                ← C1 vs C4: 衡量组合效果

  每个配置记录:
    - VRA (总体验证准确率)
    - Time distribution: PGD_time / CROWN_time / BaB_time
    - 各层筛选命中率: PGD_hit_rate, CROWN_safe_rate, CROWN_unsafe_rate
    - Total wall time (含所有层的总耗时)
```

---

## 7. 下一步代码实现计划

### Step 1: 启用 PGD 预筛选（方案 A）

**修改文件：**
- [complete_verifier/exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml)
- 新增 `exp_configs/course/attack_guided/mnist_attack_guided_kfsb5.yaml`

**新增配置内容：**
```yaml
general:
  complete_verifier: bab
  enable_incomplete_verification: true  # 保留不完整验证作为初筛
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: before          # ← 关键改动：在验证前先跑 PGD
  pgd_restarts: 5            # PGD 重启次数
  pgd_steps: 40              # PGD 迭代步数
solver:
  batch_size: 1024
bab:
  timeout: 30
  branching:
    method: kfsb
    candidates: 5
    reduceop: min
```

**测试方式：**
```bash
cd complete_verifier
python abcrown.py --config exp_configs/course/attack_guided/mnist_attack_guided_kfsb5.yaml
```

**预期结果：**
- 日志中首先出现 PGD 攻击步骤，标注 `unsafe-pgd` 的样本
- 仅剩余未被 PGD 攻破的样本进入 BaB 验证
- 总实验时间预期减少 10-20%
- VRA 不变（因为 PGD 找到的反例也会被 BaB 验证确认为 unsafe）

---

### Step 2: 运行不完整验证对比实验

**修改文件：**
- 新增 `exp_configs/course/incomplete/mnist_crown_only.yaml`
- 新增 `exp_configs/course/incomplete/mnist_alpha_crown.yaml`

**新增配置内容（CROWN-only）：**
```yaml
general:
  complete_verifier: false       # ← 仅运行不完整验证
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: skip
solver:
  batch_size: 1024
  alpha-crown:
    disable_optimization: true   # ← 禁用 alpha 优化 = 纯 CROWN
```

**alpha-CROWN 配置：**
```yaml
# 同上，但
solver:
  alpha-crown:
    disable_optimization: false  # ← 启用 alpha 优化
    lr_alpha: 0.1
    alpha_iterations: 20
```

**测试方式：**
```bash
cd complete_verifier
# 纯 CROWN
python abcrown.py --config exp_configs/course/incomplete/mnist_crown_only.yaml
# alpha-CROWN
python abcrown.py --config exp_configs/course/incomplete/mnist_alpha_crown.yaml
```

**预期结果：**
- CROWN-only：速度快（<1s/100 样本），但 VRA 低于 BaB（预期 60-80%）
- alpha-CROWN：比 CROWN 稍慢但更紧（预期 VRA 70-85%）
- 提供"速度 vs 证明能力"的完整对比数据

---

### Step 3: 编写对比汇总脚本

**新增文件：**
`项目书/scripts/summarize_strategy_comparison.py`

**功能：**
- 解析多种验证方法的日志文件
- 输出统一格式的对比 CSV
- 自动生成对比表格（markdown 格式）

**输入：** 5 种方法的日志文件路径
**输出：** `strategy_full_comparison.csv` — 包含所有方法的核心指标

**CSV 格式：**
```
method, type, VRA(%), safe, unsafe, timeout, mean_time, total_wall_time, notes
PGD, empirical, N/A, 0, 23, 0, 0.05s, 5s, attacks only, no proof
CROWN, formal-incomplete, 72.0, 72, 5, 23, 0.15s, 15s, fast but conservative
α-CROWN, formal-incomplete, 80.0, 80, 5, 15, 0.42s, 42s, tighter than CROWN
β-CROWN+BaB(kfsb5), formal-complete, 93.0, 93, 0, 7, 3.24s, 324s, best VRA
PGD+CROWN+BaB, hybrid, 93.0, 93, 0, 7, 2.80s, 280s, attack pre-filtered
```

---

### Step 4: CIFAR-10 基础实验

**修改/新增文件：**

1. **新增** `train_cifar_cnn.py` — CIFAR-10 训练脚本（参考 [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) 的模板）
   - 使用 [model_defs.py](alpha-beta-CROWN/complete_verifier/model_defs.py) 中已有的 `cifar_conv_small` 或 `cifar_marabou_small`
   - 训练 40-50 epochs
   - 导出 ONNX 格式

2. **新增** `exp_configs/course/cifar10/cifar_cnn_baseline.yaml`

```yaml
general:
  complete_verifier: bab
model:
  onnx_path: ../saved_models/cifar_cnn_small.onnx
data:
  dataset: CIFAR
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.008   # 2/255
attack:
  pgd_order: skip
solver:
  batch_size: 512   # CIFAR 模型更大，减小 batch
bab:
  timeout: 60        # CIFAR 模型更大，增加 timeout
  branching:
    method: kfsb
```

3. **新增** `项目书/scripts/run_cifar_experiments.sh`

**测试方式：**
```bash
cd complete_verifier
python abcrown.py --config exp_configs/course/cifar10/cifar_cnn_baseline.yaml
```

**预期结果：**
- CIFAR-10 的验证比 MNIST 更困难（输入维度 1024 vs 784，网络参数更多）
- VRA 预期低于 MNIST 同等 epsilon
- 验证时间预期更长

---

### Step 5: 撰写综合对比报告

整合 Step 1-4 的所有实验数据，产出课程报告的最终版本。

**报告应包含的核心表格：**

```
表1: 验证策略完整对比 (MNIST, ε=0.02)
表2: 不同 epsilon 下验证效果对比 (MNIST, kfsb5)
表3: PGD 预筛选效果分析
表4: 不完整验证 vs 完整验证 trade-off
表5: CIFAR-10 验证结果 (如有)
表6: 本实验 vs 文献基准方法
表7: 改进策略 AB 消融实验
```

---

### 实施优先级汇总

| 步骤 | 预计耗时 | 产出价值 | 对课程评分影响 |
|------|---------|---------|-------------|
| Step 1: PGD 预筛选 | 30 分钟 | 中 | 高（展示"攻击+验证"混合策略） |
| Step 2: 不完整验证对比 | 1 小时 | 高 | 高（完整覆盖"不同验证策略对比"） |
| Step 3: 汇总脚本 | 30 分钟 | 中 | 中 |
| Step 4: CIFAR-10 实验 | 3-4 小时 | 很高 | 很高（补齐题目要求的 CIFAR 数据集） |
| Step 5: 综合报告 | 2 小时 | 很高 | 很高（最终交付物） |

---

*报告基于对 `alpha-beta-CROWN` 仓库的只读深度分析生成，所有文件路径和行号引用自实际代码。*
