# 可复现实验运行指南

> 基于 `alpha-beta-CROWN` 课业分支真实文件路径，2026-05-04
> 面向对象：课程评分者、后续复现者、项目维护者

---

## 1. 环境准备

### 1.1 硬件与系统要求

| 组件 | 最低要求 | 当前实验环境 | 说明 |
|------|---------|------------|------|
| 操作系统 | Linux (WSL2 / Ubuntu 20.04+) | WSL2 Ubuntu | Windows 原生不支持 auto_LiRPA |
| GPU | NVIDIA GPU, CUDA 11.8+ | RTX 4060 Laptop | 验证过程中 GPU 可加速 CROWN 边界传播 |
| 内存 | >= 16 GB | — | eps=0.05 时 MIP 进程峰值约 8 GB |
| 磁盘 | >= 5 GB 空闲 | — | 数据 + 模型 + 日志 |

### 1.2 软件依赖安装

**前提**：仓库已克隆到本地（不含 `auto_LiRPA` 子模块，需单独安装）。

```bash
# === Step 1: 创建 Conda 环境 ===
conda create -n abcrown python=3.10 -y
conda activate abcrown

# === Step 2: 安装 PyTorch (CUDA 12.1 或 11.8) ===
# 选项 A: CUDA 12.1
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 选项 B: CUDA 11.8 (原实验环境)
pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu118

# === Step 3: 安装 auto_LiRPA (必须单独安装，子模块未初始化) ===
cd auto_LiRPA
pip install -e .
cd ..

# === Step 4: 安装其他 Python 依赖 ===
pip install -r complete_verifier/requirements.txt
pip install pandas matplotlib  # 汇总与绘图

# === Step 5: (可选) 安装 Gurobi ===
# 如果需要 LP/MIP 精化功能，需安装 Gurobi 并获取学术许可证
# pip install gurobipy
```

### 1.3 安装验证

```bash
cd complete_verifier
python abcrown.py --help
# 应输出参数列表，确认入口正常
```

### 1.4 数据准备

模型文件已包含在仓库中：
- `saved_models/mnist_fcnn.onnx` — MNIST 全连接网络（ONNX 格式）
- `saved_models/mnist_fcnn.pth` — PyTorch 权重

MNIST 数据集会在首次运行验证时自动下载到 `complete_verifier/datasets/` 目录。如果训练脚本重新执行，数据下载到 `data/MNIST/`。

---

## 2. 当前已完成实验复现

### 2.1 M2 策略对比

**实验目的**：在固定 ε=0.02 下对比 baseline / auto / kfsb 三种验证策略。

**配置文件**：

| 策略 | 配置文件（相对于 `complete_verifier/`） | 关键参数 |
|------|--------------------------------------|---------|
| baseline | [mnist_baseline.yaml](alpha-beta-CROWN/complete_verifier/mnist_baseline.yaml) | `branching.method: babsr` |
| auto | [exp_configs/course/mnist_baseline_auto.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/mnist_baseline_auto.yaml) | `branching.method: babsr`, 自动参数调优 |
| kfsb | [exp_configs/course/mnist_baseline_kfsb.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/mnist_baseline_kfsb.yaml) | `branching.method: kfsb` |

**运行命令**（推荐用批量脚本）：

```bash
cd 项目书/scripts
bash run_m2_strategy_compare.sh
```

**或手动逐条运行**：

```bash
cd complete_verifier

# baseline
python abcrown.py --config mnist_baseline.yaml \
    --start 0 --end 100 --complete_verifier bab --branching_method babsr \
    2>&1 | tee ../项目书/results/m2/logs/m2_baseline_0_100.log

# auto
python abcrown.py --config exp_configs/course/mnist_baseline_auto.yaml \
    --start 0 --end 100 --complete_verifier auto --branching_method babsr \
    2>&1 | tee ../项目书/results/m2/logs/m2_auto_0_100.log

# kfsb
python abcrown.py --config exp_configs/course/mnist_baseline_kfsb.yaml \
    --start 0 --end 100 --complete_verifier bab --branching_method kfsb \
    2>&1 | tee ../项目书/results/m2/logs/m2_kfsb_0_100.log
```

**⚠️ 注意事项**：原脚本中的 `ROOT_DIR="/home/han/alpha-beta-CROWN"` 需要修改为实际路径，或在仓库根目录单独创建一个软链接。也可跳过批量脚本，直接用上面的手动命令。

**结果汇总**：

```bash
cd 项目书/scripts
python summarize_m2_results.py
# 输出: 项目书/results/m2/m2_strategy_compare_0_100.csv
```

**生成图表**：

```bash
python plot_m2_results.py
# 输出: 项目书/results/m2/figures/m2_mean_time.png
#      项目书/results/m2/figures/m2_max_time_vra_timeout.png
```

**预期结果**：

| 策略 | VRA | Safe | Timeout | Mean Time |
|------|-----|------|---------|-----------|
| baseline | 91.0% | 91 | 9 | 3.82s |
| auto | 91.0% | 91 | 9 | 5.77s |
| kfsb | 92.0% | 92 | 8 | 3.17s |

---

### 2.2 M3 分支策略消融

**实验目的**：对分支策略的关键参数进行单变量消融实验。

**配置文件**（均在 [exp_configs/course/m3/](alpha-beta-CROWN/complete_verifier/exp_configs/course/m3/) 下）：

| 配置 | 文件名 | 消融变量 |
|------|--------|---------|
| baseline | `mnist_m3_baseline.yaml` | babsr, candidates=3, reduceop=min |
| auto | `mnist_m3_auto.yaml` | babsr, 自动参数 |
| kfsb | `mnist_m3_kfsb.yaml` | **策略 babsr→kfsb** |
| kfsb_reduceop_max | `mnist_m3_kfsb_reduceop_max.yaml` | **reduceop min→max** |
| kfsb_candidates5 | `mnist_m3_kfsb_candidates5.yaml` | **candidates 3→5** |

**运行命令**：

```bash
cd 项目书/scripts
bash run_m3_branching_ablation.sh
```

**或手动逐条**：

```bash
cd complete_verifier

for cfg in \
  exp_configs/course/m3/mnist_m3_baseline.yaml \
  exp_configs/course/m3/mnist_m3_auto.yaml \
  exp_configs/course/m3/mnist_m3_kfsb.yaml \
  exp_configs/course/m3/mnist_m3_kfsb_reduceop_max.yaml \
  exp_configs/course/m3/mnist_m3_kfsb_candidates5.yaml; do
    echo "[RUN] ${cfg}"
    python abcrown.py --config "${cfg}" \
      2>&1 | tee "../项目书/results/m3/logs/$(basename ${cfg} .yaml).log"
done
```

**结果汇总**：

```bash
cd 项目书/scripts
python summarize_m3_results.py
# 输出: 项目书/results/m3/m3_branching_ablation.csv
```

**生成图表**：

```bash
python plot_m3_results.py
# 输出: 项目书/results/m3/figures/m3_verified_acc.png
#      项目书/results/m3/figures/m3_mean_time.png
#      项目书/results/m3/figures/m3_timeout.png
```

**预期结果**（最优配置为 `kfsb_candidates5`）：

| 配置 | VRA | Timeout | Mean Time |
|------|-----|---------|-----------|
| baseline | 91.0% | 9 | 4.06s |
| auto | 91.0% | 9 | 6.14s |
| kfsb | 92.0% | 8 | 3.60s |
| kfsb_reduceop_max | 92.0% | 8 | 3.44s |
| **kfsb_candidates5** | **93.0%** | **7** | **3.24s** |

---

### 2.3 M4 epsilon 网格扫描

**实验目的**：扫描不同 ε 下各策略的验证效果趋势。

**配置文件**（均在 [exp_configs/course/m4/](alpha-beta-CROWN/complete_verifier/exp_configs/course/m4/) 下）：

共 12 个文件，命名规则 `mnist_m4_{strategy}_eps{epsilon}.yaml`：
- strategy ∈ {baseline, auto, kfsb}
- epsilon ∈ {0.01, 0.02, 0.03, 0.05}

**运行命令**：

```bash
cd 项目书/scripts
bash run_m4_epsilon_grid.sh
```

**重要说明**：原脚本对 eps=0.05 的所有策略和 kfsb+eps=0.03 使用了**分块执行**（10 样本/进程），以降低单进程内存压力和 timeout 累积效应。这是因为 eps=0.05 会导致大量 timeout，在 WSL 下长时间运行不稳定。

**手动运行（简化版，适合快速验证）**：

```bash
cd complete_verifier

STRATEGIES=("baseline" "auto" "kfsb")
EPSILONS=("0.01" "0.02" "0.03" "0.05")

for s in "${STRATEGIES[@]}"; do
  for e in "${EPSILONS[@]}"; do
    cfg="exp_configs/course/m4/mnist_m4_${s}_eps${e}.yaml"
    log="../项目书/results/m4/logs/${s}_eps${e}.log"
    echo "[RUN] ${cfg}"
    python abcrown.py --config "${cfg}" 2>&1 | tee "${log}"
  done
done
```

**结果汇总**：

```bash
cd 项目书/scripts
python summarize_m4_results.py
# 输出: 项目书/results/m4/m4_epsilon_grid.csv
```

**生成图表**：

```bash
python plot_m4_results.py
# 输出: 项目书/results/m4/figures/m4_vra_epsilon.png
#      项目书/results/m4/figures/m4_mean_time_epsilon.png
#      项目书/results/m4/figures/m4_timeout_epsilon.png
```

**预期结果**（kfsb 策略）：

| ε | VRA | Timeout | Mean Time |
|---|-----|---------|-----------|
| 0.01 | 100.0% | 0 | 0.31s |
| 0.02 | 92.0% | 8 | 3.27s |
| 0.03 | 68.0% | 32 | 6.66s |
| 0.05 | 11.0% | 89 | 11.38s |

---

## 3. 需要补充的实验一：PGD 攻击评估

### 3.1 当前状态

PGD 攻击代码**已完整实现**，具体分布在：
- [attack/attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py) — PGD 核心算法
- [attack/attack_interface.py](alpha-beta-CROWN/complete_verifier/attack/attack_interface.py) — 攻击接口
- [attack/attack_utils.py](alpha-beta-CROWN/complete_verifier/attack/attack_utils.py) — 攻击工具函数

**当前未参与实验的原因**：M2/M3/M4 的所有 YAML 配置中均设置了 `attack.pgd_order: skip`。该配置跳过了 PGD 攻击，验证器直接进入形式化验证阶段。

### 3.2 如何启用 PGD 攻击

只需在 YAML 中修改一组参数即可启用：

```yaml
# 原配置（跳过攻击）
attack:
  pgd_order: skip

# 改为（验证前先跑 PGD）
attack:
  pgd_order: before          # "before" = 在验证前攻击, "after" = 验证后攻击
  pgd_restarts: 5            # PGD 随机重启次数（默认 1）
  pgd_steps: 40              # PGD 迭代步数（默认 40）
  attack_mode: diversed      # 攻击模式：diversed / uniform / osi
```

### 3.3 推荐的新配置文件

建议在 `complete_verifier/exp_configs/course/` 下新增以下配置文件：

**`exp_configs/course/pgd_attack/mnist_pgd_baseline.yaml`**（PGD-only，用于基线评估）：

```yaml
general:
  complete_verifier: false               # ← 仅运行不完整验证 + PGD
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: before                      # ← PGD 先筛选
  pgd_restarts: 5
  pgd_steps: 40
solver:
  batch_size: 1024
```

**`exp_configs/course/pgd_attack/mnist_pgd_bab_kfsb5.yaml`**（PGD + BaB 混合验证）：

```yaml
general:
  complete_verifier: bab                  # ← PGD 筛过后，剩余样本走完整 BaB
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: before
  pgd_restarts: 5
  pgd_steps: 40
solver:
  batch_size: 1024
bab:
  timeout: 30
  branching:
    method: kfsb
    candidates: 5
    reduceop: min
```

### 3.4 运行命令

```bash
cd complete_verifier

# 仅 PGD 攻击评估
python abcrown.py \
  --config exp_configs/course/pgd_attack/mnist_pgd_baseline.yaml \
  2>&1 | tee ../项目书/results/pgd_attack/pgd_only_eps0.02.log

# PGD + BaB 混合验证
python abcrown.py \
  --config exp_configs/course/pgd_attack/mnist_pgd_bab_kfsb5.yaml \
  2>&1 | tee ../项目书/results/pgd_attack/pgd_bab_kfsb5_eps0.02.log
```

### 3.5 应记录的关键指标

| 指标 | 含义 | 日志中关键词 |
|------|------|------------|
| PGD 攻击成功率 | `unsafe-pgd 样本数 / 总样本数 × 100%` | `unsafe-pgd` |
| PGD 平均耗时 | 平均每个样本攻击时间 | `attack` timer |
| BaB 验证样本数 | 被 PGD 筛过后还剩多少样本 | `Problem instances count` |
| BaB VRA | 剩余样本的验证准确率 | `Final verified acc` |
| 总体验证准确率 | (PGD_unsafe + BaB_safe) / total | 需手动计算 |
| 总 wall time | PGD时间 + BaB时间 | 手动计时 |

**预期结果**（基于文献经验）：
- PGD 预期在 ε=0.02 时命中约 5-8% 的样本（标记为 unsafe-pgd）
- 剩余样本的 BaB VRA 应与原实验结果一致（因为 PGD 只做 unsafe 标记，不影响 safe 判定）
- 总时间节省来自 PGD 快速发现的 unsafe 样本（免去 BaB 的 30s timeout 开销）

---

## 4. 需要补充的实验二：CROWN / incomplete verification 独立对比

### 4.1 当前状态

不完整验证（CROWN / alpha-CROWN）代码**已存在且功能完整**：
- [incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py) — `incomplete_verifier()` 函数
- [beta_CROWN_solver.py](alpha-beta-CROWN/complete_verifier/beta_CROWN_solver.py) — `LiRPANet` 包装 CROWN 边界传播
- [alpha.py](alpha-beta-CROWN/complete_verifier/alpha.py) — alpha-CROWN 可学习参数优化

**当前未独立参与实验的原因**：现有 M2/M3/M4 实验中，`general.complete_verifier: bab` 强制走了完整 BaB 流程。虽然 BaB 的第一步也会运行 CROWN（不完整验证）作为初始边界，但该结果仅作为中间量，未被单独报告。

### 4.2 如何只运行不完整验证

有两种方式：

**方式一：YAML 配置（推荐）**

设置 `general.complete_verifier: false` 即可跳过 BaB，只运行不完整验证：

```yaml
general:
  complete_verifier: false              # ← 设为 false，跳过完整验证
  enable_incomplete_verification: true  # ← 启用不完整验证
```

**方式二：CLI 覆盖**

```bash
python abcrown.py --config <config>.yaml --no_complete_verifier
```

### 4.3 推荐的新配置文件

在 `complete_verifier/exp_configs/course/incomplete/` 下新增以下 YAML：

**`mnist_crown_only.yaml`**（纯 CROWN，禁用 alpha 优化）：

```yaml
general:
  complete_verifier: false
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: skip
solver:
  batch_size: 1024
  alpha-crown:
    disable_optimization: true         # ← 禁用 α 优化 = 纯 CROWN
```

**`mnist_alpha_crown.yaml`**（alpha-CROWN，启用 α 优化）：

```yaml
general:
  complete_verifier: false
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/mnist_fcnn.onnx
data:
  dataset: MNIST
  start: 0
  end: 100
specification:
  norm: .inf
  epsilon: 0.02
attack:
  pgd_order: skip
solver:
  batch_size: 1024
  alpha-crown:
    disable_optimization: false        # ← 启用 α 优化
    lr_alpha: 0.1                      # α 学习率
    alpha_iterations: 20               # α 优化迭代次数
```

**建议同时为多个 epsilon 值创建配置**（参考 M4 的网格模式）：

| 文件命名 | epsilon |
|---------|---------|
| `mnist_crown_only.yaml` | 0.02 |
| `mnist_crown_only_eps0.01.yaml` | 0.01 |
| `mnist_crown_only_eps0.03.yaml` | 0.03 |
| `mnist_alpha_crown.yaml` | 0.02 |
| `mnist_alpha_crown_eps0.01.yaml` | 0.01 |

### 4.4 运行命令

```bash
cd complete_verifier

# 纯 CROWN
python abcrown.py \
  --config exp_configs/course/incomplete/mnist_crown_only.yaml \
  2>&1 | tee ../项目书/results/incomplete/crown_only_eps0.02.log

# alpha-CROWN
python abcrown.py \
  --config exp_configs/course/incomplete/mnist_alpha_crown.yaml \
  2>&1 | tee ../项目书/results/incomplete/alpha_crown_eps0.02.log
```

### 4.5 与 BaB 完整验证的结果对比框架

建议将所有方法的结果汇总到一张对比表：

```csv
method, type, epsilon, vra, safe, unsafe, timeout, mean_time_s
CROWN, incomplete, 0.02, ?, ?, ?, ?, ?
α-CROWN, incomplete, 0.02, ?, ?, ?, ?, ?
β-CROWN+BaB(baseline), complete, 0.02, 91.0, 91, 0, 9, 3.82
β-CROWN+BaB(kfsb_c5), complete, 0.02, 93.0, 93, 0, 7, 3.24
```

**预期结果**（基于方法学原理推测）：
- CROWN-only 速度最快（< 1s / 100 样本），但 VRA 最低（预期 60-80%，epsilon 越大差距越大）
- α-CROWN 比 CROWN 更紧（预期 VRA 提高 5-10%），但速度略慢（α 优化开销）
- BaB 的 VRA 显著高于两者（对 ε=0.02: 93% vs 预计 70-80%），但速度慢 10-100×

---

## 5. 需要补充的实验三：CIFAR-10 小规模验证

### 5.1 当前仓库已有的 CIFAR-10 基础设施

| 组件 | 文件 | 状态 |
|------|------|------|
| **数据加载** | [data_utils.py:99-118](alpha-beta-CROWN/complete_verifier/data_utils.py#L99-L118) — `load_dataset()` 支持 `CIFAR` | ✅ 已就绪 |
| **CIFAR 归一化** | [data_utils.py:36-53](alpha-beta-CROWN/complete_verifier/data_utils.py#L36-L53) — `preprocess_cifar()` | ✅ 已就绪 |
| **CIFAR 预采样数据** | [data_utils.py:55-77](alpha-beta-CROWN/complete_verifier/data_utils.py#L55-L77) — `load_cifar_sample_data()` | ✅ 已就绪（需数据集文件） |
| **小型 CIFAR CNN 模型** | [model_defs.py:568-581](alpha-beta-CROWN/complete_verifier/model_defs.py#L568-L581) — `cifar_marabou_small()` | ✅ 已就绪 |
| **小型 CIFAR CNN 模型** | [model_defs.py:522-533](alpha-beta-CROWN/complete_verifier/model_defs.py#L522-L533) — `cifar_conv_small()` | ✅ 已就绪 |
| **基准 CIFAR 模型** | [model_defs.py:423-435](alpha-beta-CROWN/complete_verifier/model_defs.py#L423-L435) — `cifar_model_base()` | ✅ 已就绪 |
| **CIFAR ResNet 模型** | [models/cifar10_resnet/resnet.py](alpha-beta-CROWN/complete_verifier/models/cifar10_resnet/resnet.py) — `resnet2b()` / `resnet4b()` | ✅ 已就绪 |
| **CIFAR ResNet 评估** | [models/cifar10_resnet/cifar_eval.py](alpha-beta-CROWN/complete_verifier/models/cifar10_resnet/cifar_eval.py) | ✅ 已就绪（仅干净准确率） |
| **CIFAR 验证配置示例** | [exp_configs/tutorial_examples/cifar10_resnet2b_instances.csv](alpha-beta-CROWN/complete_verifier/exp_configs/tutorial_examples/cifar10_resnet2b_instances.csv) | ✅ 存在（VNN-COMP 格式） |

### 5.2 当前缺失的部分

| 缺失项 | 说明 |
|--------|------|
| **CIFAR-10 训练脚本** | 没有像 `train_mnist_fcnn.py` 那样针对 CIFAR 小型 CNN 的训练脚本 |
| **CIFAR-10 训练好的模型权重** | `saved_models/` 中仅有 MNIST FCNN 模型，无 CIFAR 模型 |
| **CIFAR-10 验证 YAML 配置** | 无课业式 CIFAR 验证配置（如 `cifar_baseline.yaml`） |
| **CIFAR-10 实验脚本** | 无类似 `run_m2_strategy_compare.sh` 的 CIFAR 批量实验脚本 |

### 5.3 最小可行实验方案（推荐 `cifar_marabou_small` 模型）

**为什么选 `cifar_marabou_small`**：
- 网络规模小（3 Conv + 4 ReLU + 3 Linear），参数约 80K
- ReLU 纯激活，天然适合 CROWN 类方法
- Marabou 基准模型，可对接 Marabou 对照
- 验证时间应在可接受范围（预计 < 60s/样本）

**Step 1: 训练 CIFAR-10 模型**

创建 `train_cifar_cnn.py`（仓库根目录），参考 [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) 的模板，主要改动：

```python
import torch
import torch.nn as nn
from torchvision import datasets, transforms
import sys, os
sys.path.insert(0, 'complete_verifier')
from complete_verifier.model_defs import cifar_marabou_small

# 数据加载（CIFAR-10 RGB 32x32）
transform_train = transforms.Compose([
    transforms.RandomCrop(32, padding=4),
    transforms.RandomHorizontalFlip(),
    transforms.ToTensor(),
])
transform_test = transforms.Compose([transforms.ToTensor()])

train_set = datasets.CIFAR10('./data', train=True, download=True, transform=transform_train)
test_set  = datasets.CIFAR10('./data', train=False, download=True, transform=transform_test)

# 模型
model = cifar_marabou_small()  # 来自 model_defs.py
criterion = nn.CrossEntropyLoss()
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)

# 训练 40 epochs（CIFAR-10 比 MNIST 需要更多 epoch）
# ... 标准训练循环 ...

# 保存
torch.save(model.state_dict(), "saved_models/cifar_marabou_small.pth")

# 导出 ONNX
dummy_input = torch.randn(1, 3, 32, 32)
torch.onnx.export(model, dummy_input, "saved_models/cifar_marabou_small.onnx",
                  opset_version=11, input_names=['input'], output_names=['output'])
```

**Step 2: 评估干净准确率**

```python
# 在测试集上评估，预期 CIFAR-10 干净准确率 65-75%（小模型）
```

**Step 3: 创建 CIFAR 验证 YAML 配置**

`complete_verifier/exp_configs/course/cifar10/cifar_marabou_small_baseline.yaml`：

```yaml
general:
  complete_verifier: bab
  enable_incomplete_verification: true
model:
  onnx_path: ../saved_models/cifar_marabou_small.onnx
  name: cifar_marabou_small
data:
  dataset: CIFAR             # ← 使用通用 CIFAR 加载器
  mean: [0.4914, 0.4822, 0.4465]
  std: [0.2471, 0.2435, 0.2616]
  start: 0
  end: 50                    # 先用 50 样本测试
specification:
  norm: .inf
  epsilon: 0.00784313725     # 2/255（CIFAR-10 标准最小扰动半径）
attack:
  pgd_order: skip
solver:
  batch_size: 256            # CIFAR 模型更大，减半 batch
bab:
  timeout: 60                # 增加 timeout
  branching:
    method: kfsb
    candidates: 3
    reduceop: min
```

**CIFAR-10 不同 epsilon 的推荐值**：

| ε (L∞) | ε (数值) | 在 YAML 中的写法 | 推荐理由 |
|---------|---------|----------------|---------|
| 2/255 | ≈0.008 | `epsilon: 0.00784313725` | 标准基准，绝大多数验证工具使用 |
| 4/255 | ≈0.016 | `epsilon: 0.0156862745` | 中等扰动，可观察 VRA 下降 |
| 8/255 | ≈0.031 | `epsilon: 0.031372549` | 大扰动，考验可扩展性 |

**Step 4: 运行验证**

```bash
cd complete_verifier

python abcrown.py \
  --config exp_configs/course/cifar10/cifar_marabou_small_baseline.yaml \
  2>&1 | tee ../项目书/results/cifar10/cifar_marabou_small_eps2_255.log
```

**Step 5: 复制实验到不同 epsilon**

创建对应配置文件并批量运行（参考 M4 的 shell 脚本模式）。

---

## 6. 实验矩阵

### 6.1 已完成实验

| 数据集 | 模型 | 方法 | ε | 样本数 | 配置文件 | 运行脚本 | 输出路径 | 状态 |
|--------|------|------|---|--------|---------|---------|---------|------|
| MNIST | FCNN_ReLU (3层) | BaB + babsr (baseline) | 0.02 | 0-100 | [mnist_baseline.yaml](alpha-beta-CROWN/complete_verifier/mnist_baseline.yaml) | [run_m2_strategy_compare.sh](alpha-beta-CROWN/项目书/scripts/run_m2_strategy_compare.sh) | [results/m2/logs/](alpha-beta-CROWN/项目书/results/m2/logs/) | ✅ 已完成 |
| MNIST | FCNN_ReLU (3层) | BaB + babsr (auto) | 0.02 | 0-100 | [exp_configs/course/mnist_baseline_auto.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/mnist_baseline_auto.yaml) | 同上 | 同上 | ✅ 已完成 |
| MNIST | FCNN_ReLU (3层) | BaB + kfsb | 0.02 | 0-100 | [exp_configs/course/mnist_baseline_kfsb.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/mnist_baseline_kfsb.yaml) | 同上 | 同上 | ✅ 已完成 |
| MNIST | FCNN_ReLU (3层) | 5 种消融配置 | 0.02 | 0-100 | [exp_configs/course/m3/*.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/m3/) | [run_m3_branching_ablation.sh](alpha-beta-CROWN/项目书/scripts/run_m3_branching_ablation.sh) | [results/m3/logs/](alpha-beta-CROWN/项目书/results/m3/logs/) | ✅ 已完成 |
| MNIST | FCNN_ReLU (3层) | 3策略×4ε | 0.01/0.02/0.03/0.05 | 0-100 | [exp_configs/course/m4/*.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/m4/) | [run_m4_epsilon_grid.sh](alpha-beta-CROWN/项目书/scripts/run_m4_epsilon_grid.sh) | [results/m4/logs/](alpha-beta-CROWN/项目书/results/m4/logs/) | ✅ 已完成 |

### 6.2 需要补充的实验

| 数据集 | 模型 | 方法 | ε | 样本数 | 需新建的配置文件 | 需新建的运行脚本 | 输出路径 | 状态 |
|--------|------|------|---|--------|---------------|---------------|---------|------|
| MNIST | FCNN_ReLU (3层) | PGD 攻击 | 0.02 | 0-100 | [exp_configs/course/pgd_attack/mnist_pgd_baseline.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/pgd_attack/)（新建） | 新建 `run_pgd_attack.sh` | [results/pgd_attack/](alpha-beta-CROWN/项目书/results/pgd_attack/)（新建） | ❌ 待补充 |
| MNIST | FCNN_ReLU (3层) | PGD + BaB 混合 | 0.02 | 0-100 | [exp_configs/course/pgd_attack/mnist_pgd_bab_kfsb5.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/pgd_attack/)（新建） | 同上 | 同上 | ❌ 待补充 |
| MNIST | FCNN_ReLU (3层) | CROWN only | 0.01/0.02/0.03 | 0-100 | [exp_configs/course/incomplete/mnist_crown_only*.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/incomplete/)（新建） | 新建 `run_incomplete.sh` | [results/incomplete/](alpha-beta-CROWN/项目书/results/incomplete/)（新建） | ❌ 待补充 |
| MNIST | FCNN_ReLU (3层) | α-CROWN only | 0.01/0.02/0.03 | 0-100 | [exp_configs/course/incomplete/mnist_alpha_crown*.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/incomplete/)（新建） | 同上 | 同上 | ❌ 待补充 |
| **CIFAR-10** | `cifar_marabou_small` | BaB + kfsb | 2/255, 4/255 | 0-50 | [exp_configs/course/cifar10/cifar_marabou_small_baseline.yaml](alpha-beta-CROWN/complete_verifier/exp_configs/course/cifar10/)（新建） | 新建 `run_cifar10.sh` | [results/cifar10/](alpha-beta-CROWN/项目书/results/cifar10/)（新建） | ❌ 待补充 |
| **CIFAR-10** | `cifar_marabou_small` | BaB + kfsb + candidates5 | 2/255 | 0-50 | 新建 `cifar_marabou_small_kfsb5.yaml` | 同上 | 同上 | ❌ 待补充 |

---

## 7. 结果汇总方式

### 7.1 总体流程

```
运行实验 (.sh)
    │
    ▼
日志文件 (.log)              ← 保存在 项目书/results/{m2,m3,m4,...}/logs/
    │
    ▼  summarize_*.py
CSV 汇总 (.csv)              ← 保存在 项目书/results/{m2,m3,m4}/
    │
    ▼  plot_*.py
PNG 图表 (.png)              ← 保存在 项目书/results/{m2,m3,m4}/figures/
    │
    ▼
中文报告 (.md)               ← 引用 CSV 和 PNG 路径
```

### 7.2 现有汇总脚本一览

| 脚本 | 输入 | 输出 | 依赖 |
|------|------|------|------|
| [summarize_m2_results.py](alpha-beta-CROWN/项目书/scripts/summarize_m2_results.py) | `results/m2/logs/m2_*_0_100.log` 或 `实验日志/2026-03-14_mnist_*_0_100.log` | `results/m2/m2_strategy_compare_0_100.csv` | pandas |
| [summarize_m3_results.py](alpha-beta-CROWN/项目书/scripts/summarize_m3_results.py) | `results/m3/logs/*.log` | `results/m3/m3_branching_ablation.csv` | pandas |
| [summarize_m3_nodes.py](alpha-beta-CROWN/项目书/scripts/summarize_m3_nodes.py) | `results/m3/logs/*.log` | `results/m3/m3_nodes_summary.csv` | pandas |
| [summarize_m4_results.py](alpha-beta-CROWN/项目书/scripts/summarize_m4_results.py) | `results/m4/logs/*.log` | `results/m4/m4_epsilon_grid.csv` | pandas |

**每个汇总脚本的核心逻辑**（以 [summarize_m4_results.py](alpha-beta-CROWN/项目书/scripts/summarize_m4_results.py) 为例）：

```python
# 1. 遍历 LOG_DIR 下的 .log 文件
# 2. 用正则提取关键指标：
FINAL_ACC_RE = r"Final verified acc:\s*([0-9]*\.?[0-9]+)%"
COUNT_RE = r"Problem instances count:\s*(\d+).*safe.*:(\d+).*unsafe.*:(\d+).*timeout:\s*(\d+)"
TIME_RE = r"mean time.*:([0-9]*\.?[0-9]+).*max time:\s*([0-9]*\.?[0-9]+)"
NAME_RE = r"(baseline|auto|kfsb)_eps([0-9.]+)\.log$"  # 从文件名解析策略和 epsilon

# 3. 输出 CSV
df.to_csv(OUT_CSV, index=False, encoding="utf-8-sig")
```

### 7.3 现有绘图脚本一览

| 脚本 | 输入 CSV | 输出 PNG | 图表内容 |
|------|---------|---------|---------|
| [plot_m2_results.py](alpha-beta-CROWN/项目书/scripts/plot_m2_results.py) | `m2_strategy_compare_0_100.csv` | `m2_mean_time.png`, `m2_max_time_vra_timeout.png` | 策略对比柱状图 |
| [plot_m3_results.py](alpha-beta-CROWN/项目书/scripts/plot_m3_results.py) | `m3_branching_ablation.csv` | `m3_verified_acc.png`, `m3_mean_time.png`, `m3_timeout.png` | 消融实验柱状图 |
| [plot_m4_results.py](alpha-beta-CROWN/项目书/scripts/plot_m4_results.py) | `m4_epsilon_grid.csv` | `m4_vra_epsilon.png`, `m4_mean_time_epsilon.png`, `m4_timeout_epsilon.png` | ε 趋势折线图 |

### 7.4 新实验的汇总与绘图扩展

对于第 3/4/5 节的新实验，可以**复用现有脚本模式**：

**PGD 实验结果汇总**（新建 `summarize_pgd_results.py`）：

```python
# 解析 PGD 日志中的 "unsafe-pgd" 计数和剩余样本的 BaB 指标
# 输出列: method, epsilon, pgd_unsafe, bab_safe, bab_unsafe, bab_timeout, pgd_time, bab_mean_time, total_wall_time
```

**不完整验证结果汇总**（新建 `summarize_incomplete_results.py`）：

```python
# 解析不完整验证日志
# 输出列: method, epsilon, vra, safe, unsafe, unknown, mean_time
# 可与 M2/M3/M4 的 BaB 结果合并为一张对比表
```

### 7.5 最终报告应引用的结果文件

| 报告章节 | 应引用的文件 |
|---------|------------|
| 模型与数据 | `train_mnist_fcnn.py`, `saved_models/mnist_fcnn.onnx` |
| 策略对比 | `results/m2/m2_strategy_compare_0_100.csv` + `figures/m2_*.png` |
| 消融实验 | `results/m3/m3_branching_ablation.csv` + `figures/m3_*.png` |
| Epsilon 趋势 | `results/m4/m4_epsilon_grid.csv` + `figures/m4_*.png` |
| 攻击评估 | `results/pgd_attack/*.csv`（待生成） |
| 不完整 vs 完整 | `results/incomplete/*.csv`（待生成） |
| CIFAR-10 | `results/cifar10/*.csv`（待生成） |
| 改进方法验证 | `results/m3/m3_nodes_summary.csv`（节点开销数据） |

---

### 附录 A: 脚本中的硬编码路径问题

当前所有 Shell 脚本和 Python 汇总脚本中的 `ROOT_DIR="/home/han/alpha-beta-CROWN"` 是**硬编码**的，在不同机器上需要修改。两种解决方法：

**方法一（推荐）**：在仓库根目录创建符号链接
```bash
sudo ln -s /你的/实际/路径/alpha-beta-CROWN /home/han/alpha-beta-CROWN
```

**方法二**：修改所有脚本中的 `ROOT_DIR`，或改为相对路径。例如将：
```bash
ROOT_DIR="/home/han/alpha-beta-CROWN"
```
改为：
```bash
ROOT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"  # 自动推导为仓库根目录
```

### 附录 B: 预计总运行时间

| 实验 | 样本数 | 预计耗时 | 说明 |
|------|--------|---------|------|
| M2 (3 策略) | 100×3 | ~30 分钟 | GPU 加速 |
| M3 (5 配置) | 100×5 | ~40 分钟 | GPU 加速 |
| M4 (12 配置) | 100×12 | ~2 小时 | eps=0.05 分块执行 |
| PGD 攻击 | 100×2 | ~10 分钟 | CPU-bound |
| CROWN/α-CROWN | 100×2 | ~5 分钟 | GPU 加速 |
| CIFAR-10 训练 | 50,000 | ~30 分钟 | GPU 训练 40 epochs |
| CIFAR-10 验证 | 50×3×2ε | ~1-2 小时 | 模型更大，timeout 更长 |
| **合计（已完成+补充）** | — | **~5-6 小时** | 可分阶段执行 |

---

*本指南基于 `alpha-beta-CROWN` 仓库实际文件路径生成，所有引用的脚本、配置、代码均经过路径验证。*
