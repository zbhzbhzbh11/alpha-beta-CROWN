# 仓库结构扫描报告

> 扫描时间：2026-05-04
> 扫描范围：`d:\桌面\大作业\软件质量保障\project\alpha-beta-CROWN`（只读）
> 版本：基于 GitHub `zbhzbhzbh11/alpha-beta-CROWN` 的课业分支

---

## 1. 项目总体判断

**当前仓库属于：混合验证实验项目（课程作业型）**

判定依据：

- 仓库基于上游 alpha-beta-CROWN（国际竞赛级验证工具）完整 Fork，保留了核心验证引擎（BaB + CROWN 边界传播 + PGD 攻击 + LP/MIP 求解）。
- 但当前实际完成的实验内容聚焦于 **单一模型（MNIST FCNN）+ 分支策略对比 + epsilon 网格扫描**，属于"工具配置层面的对比实验"。
- 项目明确定位为课程作业，以可复现的"配置执行-日志归档-结果汇总-图表生成"流程为核心产出，而非提出原创算法。
- 仓库在 [README.md](alpha-beta-CROWN/README.md) 中明确声明"本项目聚焦于全连接网络（FCNN）的验证策略优化"，并解释"MNIST 的 FCNN 模型已足够展示策略差异；CIFAR-10 通常使用 CNN 架构，不在本项目范围内"。

**定位对比：**

| 维度 | 当前实际 | 课程题目期望 |
|------|---------|------------|
| 实验类型 | 分支策略消融 + epsilon 趋势 | "对比不同验证策略" |
| 数据集 | 仅 MNIST | "MNIST、CIFAR-10" |
| 模型类型 | 仅 FCNN | "分类网络"（含 CNN） |
| 外部工具对比 | 未实际接入 | "参考 Marabou 或 α,β-CROWN 等工具" |
| 改进策略 | 有（kfsb+candidates=5） | "鼓励提出更优的验证思想" |

**结论：当前已完成课程题目约 60% 的核心工作，在"策略对比"和"epsilon 扫描"方面数据充分，但缺少 CIFAR-10 实验、缺少外部工具横向对比、缺少 Marabou 等对照实验。**

---

## 2. 核心目录与文件说明

| 文件/目录 | 作用 | 与课程题目的关系 | 是否关键 |
|-----------|------|-----------------|---------|
| [complete_verifier/abcrown.py](alpha-beta-CROWN/complete_verifier/abcrown.py) | 主入口，协调验证全流程 | 核心验证入口，所有实验基于此运行 | **关键** |
| [complete_verifier/bab.py](alpha-beta-CROWN/complete_verifier/bab.py) | 分支定界主循环，完整验证核心 | 实现 BaB 完整验证，对应课程"分支定界"思想 | **关键** |
| [complete_verifier/beta_CROWN_solver.py](alpha-beta-CROWN/complete_verifier/beta_CROWN_solver.py) | CROWN/alpha-CROWN 边界传播包装器 | 实现 CROWN 和 alpha-CROWN 线性松弛验证 | **关键** |
| [complete_verifier/alpha.py](alpha-beta-CROWN/complete_verifier/alpha.py) | alpha-CROWN 可学习参数管理 | alpha-CROWN 优化核心 | **关键** |
| [complete_verifier/beta.py](alpha-beta-CROWN/complete_verifier/beta.py) | beta-CROWN 分割约束参数 | beta-CROWN 约束优化 | **关键** |
| [complete_verifier/incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py) | 不完整验证（仅 CROWN 边界传播） | 快速下界估计，与完整验证形成对比 | **关键** |
| [complete_verifier/specifications.py](alpha-beta-CROWN/complete_verifier/specifications.py) | VNNLIB 规范生成（epsilon 转形式化目标） | 将扰动半径转为可验证规范 | **关键** |
| [complete_verifier/model_defs.py](alpha-beta-CROWN/complete_verifier/model_defs.py) | ~50 种模型架构定义 | 含 MNIST FCNN、CIFAR CNN、CROWN-IBP 等 | **关键** |
| [complete_verifier/heuristics/](alpha-beta-CROWN/complete_verifier/heuristics/) | BaB 分支启发式（babsr/fsb/kfsb 等） | **对应课程"不同验证策略"的核心差异点** | **关键** |
| [complete_verifier/attack/attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py) | PGD 对抗攻击实现 | 对抗攻击评估，经验验证方法 | **关键** |
| [complete_verifier/attack/bab_attack.py](alpha-beta-CROWN/complete_verifier/attack/bab_attack.py) | 基于 BaB 的强化攻击 | 寻找更强反例 | 重要 |
| [complete_verifier/lp_mip_solver/](alpha-beta-CROWN/complete_verifier/lp_mip_solver/) | Gurobi LP/MIP 精确求解 | 对应课程"SMT/MILP 验证"方向 | 重要 |
| [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) | MNIST FCNN 训练 + ONNX 导出 | 唯一训练脚本，产出验证靶标模型 | **关键** |
| [saved_models/mnist_fcnn.onnx](alpha-beta-CROWN/saved_models/mnist_fcnn.onnx) | 训练好的 MNIST FCNN ONNX 模型 | 实验靶标 | **关键** |
| [complete_verifier/arguments.py](alpha-beta-CROWN/complete_verifier/arguments.py) | 完整参数配置系统 | 控制所有实验变量 | **关键** |
| [complete_verifier/data_utils.py](alpha-beta-CROWN/complete_verifier/data_utils.py) | MNIST/CIFAR 数据加载 | 含多种数据集加载器 | 重要 |
| [complete_verifier/exp_configs/course/](alpha-beta-CROWN/complete_verifier/exp_configs/course/) | 课业实验配置文件（M2/M3/M4） | 实验复现的核心配置 | **关键** |
| [项目书/scripts/run_m2_strategy_compare.sh](alpha-beta-CROWN/项目书/scripts/run_m2_strategy_compare.sh) | M2 策略对比批量运行脚本 | 可复现实验入口 | **关键** |
| [项目书/scripts/run_m3_branching_ablation.sh](alpha-beta-CROWN/项目书/scripts/run_m3_branching_ablation.sh) | M3 消融实验批量脚本 | 可复现实验入口 | **关键** |
| [项目书/scripts/run_m4_epsilon_grid.sh](alpha-beta-CROWN/项目书/scripts/run_m4_epsilon_grid.sh) | M4 epsilon 网格批量脚本 | 可复现实验入口 | **关键** |
| [项目书/scripts/summarize_m*.py](alpha-beta-CROWN/项目书/scripts/) | 日志解析 → CSV 汇总 | 结果处理 | 重要 |
| [项目书/scripts/plot_m*.py](alpha-beta-CROWN/项目书/scripts/) | CSV → 可视化图表 | 结果呈现 | 重要 |
| [项目书/results/m2/m2_strategy_compare_0_100.csv](alpha-beta-CROWN/项目书/results/m2/m2_strategy_compare_0_100.csv) | M2 结果数据 | 策略对比最终结论 | **关键** |
| [项目书/results/m3/m3_branching_ablation.csv](alpha-beta-CROWN/项目书/results/m3/m3_branching_ablation.csv) | M3 消融结果 | 最佳配置对比 | **关键** |
| [项目书/results/m4/m4_epsilon_grid.csv](alpha-beta-CROWN/项目书/results/m4/m4_epsilon_grid.csv) | M4 epsilon 扫描结果 | 扰动半径影响 | **关键** |
| [项目书/软件学报风格论文初稿.md](alpha-beta-CROWN/项目书/软件学报风格论文初稿.md) | 课程论文初稿 | 交付物 | 重要 |
| [项目书/开题报告.md](alpha-beta-CROWN/项目书/开题报告.md) | 开题报告 | 项目规划文档 | 重要 |
| [项目书/results/结果汇总报告_2026-04-04.md](alpha-beta-CROWN/项目书/results/结果汇总报告_2026-04-04.md) | 中文结果汇总报告 | 课程报告交付物 | 重要 |
| auto_LiRPA/ | Git 子模块（未初始化） | 线性边界传播底层库 | **关键**（需手动安装） |
| [complete_verifier/exp_configs/beta_crown/](alpha-beta-CROWN/complete_verifier/exp_configs/beta_crown/) | beta-CROWN 求解器配置（含 CIFAR） | 包含 CIFAR 验证配置，**但未被课业实验使用** | 潜在可用 |
| [complete_verifier/models/cifar10_resnet/](alpha-beta-CROWN/complete_verifier/models/cifar10_resnet/) | CIFAR-10 ResNet 模型与评估 | CIFAR-10 基础设施存在，**但未用于验证实验** | 潜在可用 |

---

## 3. 模型结构分析

### 3.1 当前已有模型

**实验使用的靶标模型：**
- [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) 定义的 `FCNN_ReLU`：
  - 结构：`Flatten` → `Linear(784, 256)` → `ReLU` → `Linear(256, 128)` → `ReLU` → `Linear(128, 10)`
  - 参数量：约 237K
  - 输入：28×28 (MNIST)
  - 保存格式：`.pth` (PyTorch) + `.onnx` (ONNX)
  - 训练 epoch：5

**代码库中存在但未被课业实验使用的模型（在 [model_defs.py](alpha-beta-CROWN/complete_verifier/model_defs.py) 中定义）：**

| 模型类别 | 代表模型 | ReLU | 适配 MNIST | 适配 CIFAR-10 |
|---------|---------|------|-----------|-------------|
| MNIST FCNN 系列 | `mnist_fc_6_200`, `mnist_fc_7_512` 等 ~15 种 | ✅ | ✅ | ❌ |
| MNIST CNN 系列 | `mnist_conv_small`, `MadryCNN` 等 ~8 种 | ✅ | ✅ | ❌ |
| CIFAR CNN 系列 | `cifar_conv_small`, `cifar_conv_big` 等 ~10 种 | ✅ | ❌ | ✅ |
| CIFAR ResNet 系列 | `resnet2b`, `resnet4b` | ✅ | ❌ | ✅ |
| CROWN-IBP 系列 | `crown_ibp_model_a_b` 等 ~10 种 | ✅ | ✅ | ✅ |
| TradesCNN 系列 | `TradesCNN` 变体 3 种 | ✅ | ✅ | ✅ |
| CIFAR Marabou 系列 | `cifar_marabou_small/medium/large` | ✅ | ❌ | ✅ |

### 3.2 ReLU 使用情况

所有实验使用的模型均使用 ReLU 激活函数，满足题目要求。训练脚本 [train_mnist_fcnn.py:8](alpha-beta-CROWN/train_mnist_fcnn.py#L8) 明确注释"严格遵循题目要求：FCNN + ReLU"，模型中每个 Linear 层后紧跟 `nn.ReLU()`（[train_mnist_fcnn.py:12-17](alpha-beta-CROWN/train_mnist_fcnn.py#L12-L17)）。

### 3.3 形式化验证适用性

- **当前模型完全适合形式化验证**：3 层全连接、仅 784×256 + 256×128 + 128×10 = 235K 参数、纯 ReLU 激活。
- ReLU 的分段线性特性使其适合 CROWN 线性松弛和 BaB 验证。
- 模型已导出 ONNX 格式，可直接由 abcrown.py 入口加载。
- 网络规模极小，**非常适合 Marabou 或 α,β-CROWN 类工具验证**（验证时间在秒级到分钟级）。

### 3.4 数据集支持

- **MNIST：已完整支持**。训练脚本使用 `torchvision.datasets.MNIST`，验证配置使用 `dataset: MNIST`，数据加载器 [data_utils.py](alpha-beta-CROWN/complete_verifier/data_utils.py) 包含 `load_mnist_sample_data()`。
- **CIFAR-10：基础设施存在但未使用**。[data_utils.py](alpha-beta-CROWN/complete_verifier/data_utils.py) 包含 `load_cifar_sample_data()`，[model_defs.py](alpha-beta-CROWN/complete_verifier/model_defs.py) 包含数十种 CIFAR 模型，[exp_configs/beta_crown/](alpha-beta-CROWN/complete_verifier/exp_configs/beta_crown/) 包含 CIFAR 验证配置。**但课业实验中无任何 CIFAR-10 验证实验痕迹。**

---

## 4. 当前验证策略分析

### 4.1 不完整验证（Incomplete Verification）— CROWN / alpha-CROWN

| 维度 | 描述 |
|------|------|
| 方法名称 | CROWN（线性松弛边界传播）/ alpha-CROWN（可学习参数优化版） |
| 所在文件 | [incomplete_verifier_func.py](alpha-beta-CROWN/complete_verifier/incomplete_verifier_func.py)、[beta_CROWN_solver.py](alpha-beta-CROWN/complete_verifier/beta_CROWN_solver.py)、[alpha.py](alpha-beta-CROWN/complete_verifier/alpha.py) |
| 核心思想 | 通过线性松弛将 ReLU 激活函数替换为线性上下界，逐层传播得到输出上下界。若对所有 $j \ne y$ 均有 $\text{lb}_y - \text{ub}_j > 0$，则可证明鲁棒性。alpha-CROWN 通过梯度下降优化松弛参数以收紧边界。 |
| 类型 | **形式化验证（不完整）** — 能证明安全但未必能发现所有不安全样本 |
| 优点 | 速度快（GPU 友好）、可批量处理、对浅层网络效果好（ε=0.01 时 100% 验证准确率） |
| 局限 | 线性松弛引入过近似，大 epsilon 下边界过于松弛，无法证明安全性（ε=0.05 时仅 5%-12%） |
| 与 Marabou / α,β-CROWN 对应 | 对应 α,β-CROWN 中的 **CROWN 和 alpha-CROWN**；与 Marabou 的 Reluplex（基于 Simplex）不同，CROWN 使用线性松弛而非精确求解 |

### 4.2 完整验证（Complete Verification）— Branch and Bound (BaB)

| 维度 | 描述 |
|------|------|
| 方法名称 | β-CROWN 分支定界（BaB with α-CROWN bounds） |
| 所在文件 | [bab.py](alpha-beta-CROWN/complete_verifier/bab.py)、[complete_verifier_func.py](alpha-beta-CROWN/complete_verifier/complete_verifier_func.py)、[beta.py](alpha-beta-CROWN/complete_verifier/beta.py) |
| 核心思想 | 对不确定的 ReLU 神经元进行分支（split），在子域上重新运行 CROWN/alpha-CROWN 边界传播，递归缩小不确定区域直到所有子域确定安全或不安全。β-CROWN 引入分割约束到边界传播中以进一步收紧。 |
| 类型 | **形式化验证（完整）** — 理论上可以证明所有安全样本，也可找到所有反例 |
| 优点 | 能证明更多样本（ε=0.02 时 baseline 达 91%，kfsb 最优达 93%） |
| 局限 | 计算成本随 ReLU 神经元数和 epsilon 增长而指数上升（ε=0.05 时 timeout 达 88-95/100） |
| 与 Marabou / α,β-CROWN 对应 | 对应 α,β-CROWN 中的 **β-CROWN + BaB**；Marabou 同样使用分支定界框架，但底层使用 Reluplex 的 Simplex 精化而非 Gurobi/CROWN 传播 |

### 4.3 分支策略对比（baseline vs auto vs kfsb）— 当前实验核心

| 策略名称 | 所在文件 | 核心思想 | 实验表现 |
|---------|---------|---------|---------|
| **baseline (babsr)** | [heuristics/babsr.py](alpha-beta-CROWN/complete_verifier/heuristics/babsr.py) | 基于 BaBSR 评分选择对边界收紧贡献最大的 ReLU 神经元进行分支 | ε=0.02: 91.0%, 平均 4.06s, 9 timeout |
| **auto** | 同上 + auto_LiRPA 自动选择 | 自动参数调优的 babsr 策略 | ε=0.02: 91.0%, 平均 6.14s, 9 timeout |
| **kfsb (k-Filtered Smart Branching)** | [heuristics/kfsb.py](alpha-beta-CROWN/complete_verifier/heuristics/kfsb.py) | 在 babsr 基础上增加候选过滤机制，从更多候选（candidates=3/5）中选择最优分支点 | ε=0.02: 92.0%, 平均 3.60s, 8 timeout |
| **kfsb_candidates5** (最优) | 同上，candidates=5 | 扩展候选池到 5 个，以更多节点访问换取更高验证成功率 | ε=0.02: **93.0%**, 平均 **3.24s**, **7** timeout |

### 4.4 PGD 攻击（经验评估）

| 维度 | 描述 |
|------|------|
| 方法名称 | Projected Gradient Descent (PGD) |
| 所在文件 | [attack/attack_pgd.py](alpha-beta-CROWN/complete_verifier/attack/attack_pgd.py)、[attack/attack_interface.py](alpha-beta-CROWN/complete_verifier/attack/attack_interface.py) |
| 核心思想 | 从原始输入出发，沿损失函数梯度方向迭代扰动，每次投影回 ε-球内，寻找使模型误分类的对抗样本 |
| 类型 | **经验验证** — 只能找到反例（unsafe），无法证明安全 |
| 优点 | 实现简单、速度快 |
| 局限 | 找不到反例不代表模型安全；PGD 可能陷入局部最优 |
| 当前实验中的角色 | 在当前课业实验配置中，`attack.pgd_order: skip` —— **PGD 攻击被跳过，未参与实验** |

### 4.5 BaB 攻击（强化对抗攻击）

| 维度 | 描述 |
|------|------|
| 方法名称 | BaB-Attack（基于分支定界的攻击） |
| 所在文件 | [attack/bab_attack.py](alpha-beta-CROWN/complete_verifier/attack/bab_attack.py) |
| 核心思想 | 将 BaB 思想用于攻击侧：通过域分割和 MIP 精化寻找比 PGD 更强的对抗样本 |
| 类型 | **经验验证（增强版）** |
| 状态 | 代码存在但**未被课业实验调用** |

### 4.6 LP/MIP 精确求解

| 维度 | 描述 |
|------|------|
| 方法名称 | Gurobi LP/MIP 精确求解 |
| 所在文件 | [lp_mip_solver/](alpha-beta-CROWN/complete_verifier/lp_mip_solver/) |
| 核心思想 | 将 ReLU 网络编码为混合整数线性规划问题，使用 Gurobi 商业求解器精确求解 |
| 类型 | **形式化验证（精确）** |
| 状态 | 代码存在，但需要 Gurobi 许可证，**未被课业实验直接调用**（BaB 过程中可能作为可选精化步骤） |

### 4.7 方法对照总结

| 本仓库方法 | 对应 α,β-CROWN 论文 | 对应 Marabou | 类型 | 课业实验使用 |
|-----------|-------------------|-------------|------|------------|
| CROWN 不完整验证 | ✅ CROWN / alpha-CROWN | ❌ 不同方法（Reluplex） | 形式化（不完整） | ✅ 是 |
| BaB 完整验证 | ✅ β-CROWN + BaB | ✅ BaB 框架相同，底层不同 | 形式化（完整） | ✅ 是 |
| PGD 攻击 | ✅ 内置攻击模块 | ❌（Marabou 不专注攻击） | 经验评估 | ❌ skip |
| BaB-Attack | ✅ BaB-Attack 模块 | ❌ | 经验评估 | ❌ 未使用 |
| Gurobi MIP | ✅ LP/MIP 精化 | ✅（Marabou 用 Gurobi/SCIP） | 形式化（精确） | ❌ 未直接使用 |
| 分支启发式对比 | ✅ 多种启发式 | ✅ Marabou 含启发式 | 策略搜索 | ✅ 核心实验 |

---

## 5. 当前实验流程分析

### 5.1 训练流程

| 维度 | 状态 |
|------|------|
| 训练脚本 | [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) — **唯一训练脚本** |
| 数据集 | 仅 MNIST |
| 模型 | 3 层 FCNN (784→256→128→10) |
| 训练方式 | 标准监督学习，CrossEntropyLoss，Adam (lr=0.001)，5 epochs |
| 模型保存 | `.pth` + `.onnx` 双格式 |
| 状态 | ✅ 已完成，产物在 [saved_models/](alpha-beta-CROWN/saved_models/) |

### 5.2 普通测试流程

| 维度 | 状态 |
|------|------|
| 干净准确率测试 | [models/cifar10_resnet/cifar_eval.py](alpha-beta-CROWN/complete_verifier/models/cifar10_resnet/cifar_eval.py) — 仅针对 CIFAR-10 ResNet，**未在课业实验中使用** |
| MNIST FCNN 干净准确率 | 训练脚本输出 epoch 级 accuracy，**无独立测试脚本** |
| 状态 | ⚠️ 不完整 — MNIST FCNN 缺少正式的干净准确率测试报告 |

### 5.3 鲁棒性测试流程

| 维度 | 状态 |
|------|------|
| 形式化验证 | ✅ 通过 `abcrown.py --config <配置文件>` 完成 BaB 完整验证 |
| 验证指标 | verified accuracy、safe/unsafe/timeout 计数、mean/max time |
| M2 实验 | ✅ 3 策略 × 100 样本 × ε=0.02 |
| M3 实验 | ✅ 5 配置 × 100 样本 × ε=0.02 |
| M4 实验 | ✅ 3 策略 × 4 epsilon × 100 样本 |
| 状态 | ✅ 鲁棒性验证实验体系完整 |

### 5.4 不同 epsilon 扰动半径实验

| Epsilon | M4 baseline | M4 auto | M4 kfsb | 样本数 |
|---------|------------|---------|---------|-------|
| 0.01 | 100.0% (0.55s) | 100.0% (0.43s) | 100.0% (0.31s) | 0-100 (前100张) |
| 0.02 | 91.0% (3.93s) | 91.0% (5.36s) | 92.0% (3.27s) | 0-100 |
| 0.03 | 65.0% (14.27s) | 62.0% (22.84s) | 68.0% (6.66s) | 0-100 |
| 0.05 | 12.0% (18.79s) | 5.0% (23.37s) | 11.0% (11.38s) | 0-100（分块执行） |

数据来源：[项目书/results/m4/m4_epsilon_grid.csv](alpha-beta-CROWN/项目书/results/m4/m4_epsilon_grid.csv)

**结论：已覆盖 ε ∈ {0.01, 0.02, 0.03, 0.05}，满足"不同扰动半径"要求**。但 ε=0.05 的 timeout 率过高（88-95%），实际有效对比数据有限。

### 5.5 MNIST / CIFAR-10 实验

| 数据集 | 训练 | 干净测试 | 鲁棒性验证 |
|--------|------|---------|-----------|
| MNIST | ✅ [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) | ⚠️ 无独立脚本 | ✅ M2/M3/M4 全系列 |
| CIFAR-10 | ❌ 无训练脚本 | ⚠️ 有 eval 脚本但未使用 | ❌ 无验证实验 |

**结论：CIFAR-10 实验完全缺失**。代码库中存在 CIFAR-10 相关基础设施（模型定义、数据加载器、部分配置文件），但均未被课业实验体系使用。

### 5.6 结果保存方式

| 产物类型 | 位置 | 格式 |
|---------|------|------|
| 实验日志 | [项目书/实验日志/](alpha-beta-CROWN/项目书/实验日志/)、[项目书/results/m3/logs/](alpha-beta-CROWN/项目书/results/m3/logs/) | `.log` |
| CSV 汇总 | [项目书/results/m2/](alpha-beta-CROWN/项目书/results/m2/)、[m3/](alpha-beta-CROWN/项目书/results/m3/)、[m4/](alpha-beta-CROWN/项目书/results/m4/) | `.csv` |
| 可视化图表 | [项目书/results/m2/figures/](alpha-beta-CROWN/项目书/results/m2/figures/)、[m3/figures/](alpha-beta-CROWN/项目书/results/m3/figures/)、[m4/figures/](alpha-beta-CROWN/项目书/results/m4/figures/) | `.png` |
| 汇总报告 | [项目书/results/](alpha-beta-CROWN/项目书/results/) | `.md` |
| 论文初稿 | [项目书/软件学报风格论文初稿.md](alpha-beta-CROWN/项目书/软件学报风格论文初稿.md) | `.md` |

**结论：结果保存方式规范、完整、可追溯。** 日志→CSV→图表→报告的流水线完整。

---

## 6. 当前改进方法分析

### 6.1 是否包含"改进验证策略"：**是**

项目在 README.md 中明确提出了改进点，并在实验数据中验证了改进效果。

### 6.2 改进点详细分析

**改进一：分支策略从 babsr 升级为 kfsb**

| 维度 | baseline (babsr) | 改进 (kfsb) |
|------|-----------------|------------|
| 改进前（baseline）| BaBSR 评分 + 3 候选 → 选最优 | — |
| 改进后（kfsb）| k-Fsb 候选过滤 + 3 候选 → 选最优 | 候选筛选机制更智能 |
| 验证准确率 | 91.0% | 92.0% (+1.0%) |
| 平均时间 | 4.06s | 3.60s (-11.3%) |
| timeout | 9 | 8 (-11.1%) |

**改进二：增加候选分支数量（candidates: 3→5）**

| 维度 | kfsb (candidates=3) | kfsb + candidates=5 |
|------|---------------------|---------------------|
| 改进前 | candidates=3 | — |
| 改进后 | candidates=5 | 更广搜索空间 |
| 验证准确率 | 92.0% | **93.0%** (+1.0%) |
| 平均时间 | 3.60s | 3.24s (-10.0%) |
| timeout | 8 | **7** (-12.5%) |
| 节点访问 | 62848-72224 | 72224 (last) |

**改进对应验证流程的哪一步：** BaB 循环中的 **分支选择（branching）** 步骤。在每次分支迭代中，算法需要从当前未确定的不稳定 ReLU 神经元中选择一个进行分裂。改进作用于"如何选择最优分裂点"这一子问题。

**改进效果评估：**

| 指标 | 是否提升 |
|------|---------|
| 验证速度（mean time） | ✅ 提升 ~20% (4.06s → 3.24s) |
| 验证成功率（VRA） | ✅ 提升 +2.0% (91.0% → 93.0%) |
| bound tightness | ⚠️ 间接体现（更多样本可证明 = bound 更紧），无直接测量 |
| 可扩展性 | ⚠️ 未测试更大模型/更多样本，无法判断 |

### 6.3 如果缺失改进可提出的低成本方向

当前项目已有改进，但以下 3 个低成本方向可进一步扩展：

1. **IBP 初筛 + 验证分流**：对低 epsilon 样本先用 IBP 快速验证，仅对 IBP 无法证明的样本启动 BaB。IBP 代码已存在于 [domain_clipper.py](alpha-beta-CROWN/complete_verifier/domain_clipper.py) 和 [input_split/bounding.py](alpha-beta-CROWN/complete_verifier/input_split/bounding.py)，可直接配置启用。

2. **自适应 timeout 分配**：当前所有样本统一 30s timeout。可根据 CROWN 下界估计值动态分配 timeout——对"容易证明"的样本给短 timeout，"困难"样本给长 timeout。仅需修改配置文件中的 `bab.timeout` 和脚本逻辑。

3. **CIFAR-10 小型 CNN 快速扩展**：代码库中已有 `cifar_marabou_small`、`cifar_conv_small` 模型定义和 CIFAR 数据加载器。只需训练一个小 CNN 模型 + 编写类似 M2 的配置文件即可完成 CIFAR-10 验证实验。

---

## 7. 当前不足与风险

### 7.1 对照课程题目要求逐项检查

| 课程题目要求 | 完成状态 | 说明 |
|-------------|---------|------|
| "以现有分类网络（激活函数：ReLU）为研究对象" | ✅ 完成 | MNIST FCNN，纯 ReLU |
| "参考 Marabou 或 α,β-CROWN 等工具" | ⚠️ 半完成 | α,β-CROWN 深度使用，Marabou 仅在文档中作为参考文献提及，未实际接入或对比 |
| "对比不同验证策略的区别" | ✅ 完成 | M2 对比 3 种策略，M3 对照 5 种配置 |
| "利用不同的验证策略在 MNIST、CIFAR-10 等数据集上验证" | ⚠️ 半完成 | MNIST 完整，**CIFAR-10 完全缺失** |
| "验证网络在不同扰动半径下的鲁棒性" | ✅ 完成 | M4 ε ∈ {0.01, 0.02, 0.03, 0.05} |
| "鼓励提出更优的验证思想" | ✅ 有 | kfsb + candidates=5 相对 baseline 有提升 |
| "实现工具对比效果" | ⚠️ 部分完成 | 仅对比了同一工具内不同策略，未对比 α,β-CROWN vs Marabou vs 其他工具 |

### 7.2 具体不足清单

| 编号 | 不足 | 严重程度 | 影响 |
|------|------|---------|------|
| P1 | **无 CIFAR-10 实验** | 高 | 课程题目明确要求"MNIST、CIFAR-10 等数据集" |
| P2 | **无 Marabou 或其他外部工具对比** | 高 | 题目要求"参考 Marabou 或 α,β-CROWN 等工具，对比不同验证策略的区别" |
| P3 | **攻击评估缺失** | 中 | PGD 在实验中设为 skip，未对比"经验攻击评估 vs 形式化验证"的差异 |
| P4 | **IBP 边界传播未独立评估** | 中 | IBP 代码存在但未作为独立策略参与对比 |
| P5 | **无模型规模扩展实验** | 低 | 仅测试了 3 层 FCNN，未验证方法在更深/更宽网络上的表现 |
| P6 | **CIFAR-10 模型训练缺失** | 中 | 无 CIFAR-10 网络训练脚本，无法直接产出可验证的 CIFAR 模型 |
| P7 | **auto_LiRPA 子模块未初始化** | 中 | 新克隆后无法直接运行，需手动安装依赖 |
| P8 | **缺少不完整验证（仅 CROWN）的对比数据** | 中 | 实验只跑了完整 BaB 验证，未与不完整验证对比 |
| P9 | **ε 区间不够细** | 低 | 仅 4 个 epsilon 值（0.01/0.02/0.03/0.05），缺 0.005、0.015、0.04 等 |
| P10 | **结果汇总表缺少与文献结果的对比** | 中 | CSV 中仅有自身数据，未引用 CROWN/alpha-CROWN 论文中的基准数值 |

### 7.3 风险评估

| 风险 | 描述 |
|------|------|
| **可复现性风险** | auto_LiRPA 子模块未初始化，新用户 clone 后无法直接运行；依赖 Gurobi 许可证（免费学术版可用但需单独申请） |
| **评分风险** | 缺少 CIFAR-10 和外部工具对比可能导致课程评分降低 |
| **论文质量风险** | 仅 MNIST 单一数据集结论不利于论文说服力 |
| **时间风险** | 补 CIFAR-10 实验需训练模型+运行验证，预计需额外 4-6 小时 |

---

## 8. 后续建议执行步骤

### 8.1 优先级排序

**优先级 P0（必须补，影响课程成绩）：**

1. **补 CIFAR-10 实验**（预计 3-4 小时）
   - 训练一个小型 CIFAR-10 CNN（推荐 `cifar_conv_small` 或 `cifar_marabou_small`，已定义在 [model_defs.py](alpha-beta-CROWN/complete_verifier/model_defs.py) 中）
   - 写 CIFAR 训练脚本（可参考 [train_mnist_fcnn.py](alpha-beta-CROWN/train_mnist_fcnn.py) 模板，改为 CIFAR 数据和 CNN 模型）
   - 配置验证 YAML，运行至少 2-3 个 epsilon 的验证实验
   - 产出：CIFAR-10 干净准确率 + verified accuracy 表

2. **增加不完整验证 vs 完整验证对比**（预计 1-2 小时）
   - 为已有 MNIST 实验增加 CROWN-only（不完整验证）的对比数据
   - 产出：不完整验证 vs 完整验证的 trade-off 分析（速度 vs 证明能力）

**优先级 P1（强烈建议，提升报告质量）：**

3. **接入 Marabou 或至少做方法学对比**（预计 2-3 小时）
   - 选项 A：安装 Marabou 对同一 MNIST FCNN 模型运行验证，产出横向对比数据
   - 选项 B（更低成本）：使用文献中的 Marabou 性能数据（如 VNN-COMP 结果），做表格式的方法学对比
   - 产出：α,β-CROWN vs Marabou 对比分析表

4. **启用 PGD 攻击评估**（预计 30 分钟）
   - 将配置中的 `attack.pgd_order: skip` 改为 `attack.pgd_order: before`（在验证前先跑 PGD 攻击）
   - 对比：PGD 找到的反例 vs 形式化验证证明的 unsafe，展示经验评估的局限性

**优先级 P2（建议，锦上添花）：**

5. **细化 epsilon 网格**：在 0.01-0.03 之间增加 0.015、0.025 两个点，更精细刻画策略差异
6. **增加 IBP 作为 baseline 策略**：启用已有 IBP 代码进行独立评估
7. **测试更深网络**：使用 `mnist_fc_5_512` 等更大模型测试可扩展性
8. **初始化 auto_LiRPA 子模块**：提交 `.gitmodules` 更新，确保他人可复现

### 8.2 实验结果汇总表建议格式

```
表1: MNIST FCNN 验证策略对比 (ε=0.02, 100 样本)
| 验证方法 | 类型 | VRA(%) | Safe | Unsafe | Timeout | Mean Time(s) | Max Time(s) |
|----------|------|--------|------|--------|---------|-------------|------------|
| PGD 攻击 | 经验评估 | N/A | - | 反例数 | - | - | - |
| CROWN (不完整) | 形式化-不完整 | ? | ? | ? | ? | ? | ? |
| α-CROWN (不完整) | 形式化-不完整 | ? | ? | ? | ? | ? | ? |
| BaB+babsr (baseline) | 形式化-完整 | 91.0 | 91 | 0 | 9 | 3.82 | 49.17 |
| BaB+kfsb (candidates=5) | 形式化-完整 | 93.0 | 93 | 0 | 7 | 3.24 | - |
| Marabou (对照) | 形式化-完整 | ? | ? | ? | ? | ? | ? |

表2: CIFAR-10 CNN 验证 (不同 epsilon)
| ε | Model | VRA(%) | Timeout | Mean Time(s) |
|---|-------|--------|---------|-------------|
| 2/255 | cifar_conv_small | ? | ? | ? |
| 8/255 | cifar_conv_small | ? | ? | ? |

表3: α,β-CROWN vs Marabou 方法学对比
| 维度 | α,β-CROWN | Marabou |
|------|-----------|---------|
| 底层方法 | CROWN 线性松弛 + BaB | Reluplex (Simplex精化) + BaB |
| GPU 加速 | ✅ | ❌ (CPU only) |
| 批量验证 | ✅ batch | ❌ 逐样本 |
| 精确性 | BaB收敛到精确 | 精确 |
| 适用网络规模 | 大 | 中小 |
```

### 8.3 课程报告撰写建议

当前 [项目书/软件学报风格论文初稿.md](alpha-beta-CROWN/项目书/软件学报风格论文初稿.md) 已有基本框架。建议补充：

1. **在"实验"章节增加**：
   - 不完整验证 vs 完整验证对比实验
   - Marabou 对照实验（实际运行或文献对比）
   - CIFAR-10 实验结果

2. **在"讨论"章节增加**：
   - 为什么 CROWN 类方法比纯 SMT 方法更适合 GPU 加速的大规模验证
   - "不完整验证→完整验证"的渐进式验证策略价值
   - 当前方法的可扩展性上限（多少层/多少神经元后 timeout 主导）

3. **在"结论"章节**：
   - 将课程题目的四个维度（不同策略对比、多数据集、多 epsilon、改进验证思想）逐一回应
   - 明确标注哪些已完成、哪些部分完成、哪些留待后续

### 8.4 外部工具接入建议

| 工具 | 接入方式 | 工作量 | 产出 |
|------|---------|--------|------|
| **Marabou** | pip install maraboupy → 对同一 ONNX 模型运行 → 比较 | 2-3h | 横向对比数据 |
| **ERAN / ELINA** | 作为对照引用文献结果 | 0.5h | 文献对比表 |
| **auto_LiRPA (子模块)** | git submodule update --init → pip install | 10min | 修复可复现性 |

---

## 附录：仓库文件统计

| 类别 | 数量 |
|------|------|
| Python 文件 | ~104 |
| YAML 配置文件 | ~289 |
| Shell 脚本 | 7 |
| 预训练模型 | 2 (.onnx + .pth) |
| CSV 结果文件 | ~8 |
| Markdown 文档 | ~10 |
| 总文件数 | ~561 |

---

*报告由自动化审计 Agent 生成，基于 `d:\桌面\大作业\软件质量保障\project\alpha-beta-CROWN` 仓库的只读扫描（2026-05-04）。*
